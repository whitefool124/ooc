# OCC Codex 工作约定

## 范围

本项目只处理 OCC：Unity 战棋 Roguelite、OCC 游戏设计文档、OCC 世界观资料、美术规范与 OCC 专用预览图。

## 开发规则

- 所有任务必须直接在主工作树 `E:/数据库/OCC_Codex` 中开发；Unity 工程的绝对路径为 `E:/数据库/OCC_Codex/UnityProject`。
- 禁止为本项目创建或使用 Git 附加 worktree、Codex 临时 worktree 或其他仓库副本；开始任务前必须确认当前仓库根目录为 `E:/数据库/OCC_Codex`。
- Unity/Funplay 操作前必须确认 `Application.dataPath` 为 `E:/数据库/OCC_Codex/UnityProject/Assets`；路径不一致时停止操作并切换到主工作树，不得在错误副本中实现或验证。
- Unity 工程位于 `UnityProject/`，默认场景是 `Assets/Scenes/CombatPrototype.unity`。
- 通过 Funplay MCP 检查或修改 Unity；改动脚本后必须重新编译并验证 Console。
- 不要直接修改 `Library/`、`Logs/`、临时缓存或生成的工程文件。
- 不要在没有用户明确要求时保存场景、进入 Play Mode 或替换正式资产。

## 开发管理规则

- 飞书文档 `https://jcnlgassfnsm.feishu.cn/docx/Y1KydFPjMocJjYxMmFEcTblcnLf` 是 OCC 具体产品内容的编辑母版。用户确认飞书修改完成后，以飞书最新全文为玩法决定、页面内容、交互结果和数据口径的唯一基准，覆盖本地策划案、数据表、代码实现和专项文档中的旧产品口径。
- `Worldbuilding/策划案/OCC_项目总策划案_v1.0.md` 是飞书总策划案的本地同步镜像，也是离线工作的第一参考。发现本地镜像与飞书不一致时，先同步飞书，不得用本地旧内容反向覆盖飞书。
- 抽象的设计原则、设计理念和设计作用等判断依据只保留在本地 `AGENTS.md`、相关技能和本地规范中，制作时必须生效，但不得写入或同步到飞书；唯一例外是总策划案第6章“教程系统”，可在该章内记录总体及节点设计目的，不得扩散到其他章节。可直接约束实现与验证的系统制作方法、时长规划和美术生产规范属于系统底层细节，应保留在飞书总案并同步到本地镜像。
- 发现任何内容与总策划案不一致时，必须立即明确指出冲突内容和涉及位置，不得自行取舍、静默覆盖或继续沿用冲突口径。冲突由用户确认后，先修改总策划案，再同步数据表、实现与飞书文档。
- `Worldbuilding/归档/` 中的资料只用于追溯，不构成当前规则，不得覆盖总策划案。
- 涉及玩法、内容或美术实现时，读取总案相关章节与对应数据表；明确的小修只读受影响内容。用户已指定任务时按该范围执行；未指定下一项时从总案“后续内容顺序”选择，不自行扩展范围。
- 任务需明确目标、涉及文件/系统、验收标准和完成后解锁的下一步；可在任务回复中说明，不为例行小修另建任务文档或改写总案。同一时间只保留一个主任务。
- 任务改变内容规模、玩法决定、后续内容顺序或数据口径时，同步更新总案与受影响的数据表。纯修复且不改变上述事实时，不为记录过程改写总案。过程记录、验证证据与历史版本直接进入 `Worldbuilding/归档/`，不得在活动文档层建立并列策划源。
- 若实现改变玩法决定，先更新总策划案，再更新数据表与实现。阻塞项必须写明阻塞原因与需要的决定，不能用猜测替代产品决策。
- 新功能必须归入剧情模式、肉鸽模式或两者共用三类之一；剧情模式的探索不得引入时间压力、敌情倒计时或拖延关门机制。

## 以可玩体验为中心的迭代规则

- 以玩家实际获得的体验衡量进度。收到宽泛目标后，主动将其收敛为当前可交付的一段完整流程，写清起点、终点和完成标准；在已有授权内执行，不把任务拆解和日常技术判断推回给用户。

- 优先检查真实入口和正常使用流程。首次建立体验或修改影响主流程时，使用正常配置和真实界面验证。直接调用内部方法、构造临时状态、替换装备只能证明对应局部，不能代替完整体验验收。

- 按影响排序：阻断流程、错误结果、看不清或点不准的问题优先。每轮集中解决最重要的少数问题，其他发现记入待办，避免不断扩展当前范围。用户明确指定的优先级优先。

- 每次修改必须对应已观察到的问题，说明原因判断、预期改善和验证方式。先判断能否通过简化设计、布局或信息展示解决，再增加特殊处理。连续修改没有明显改善时，重新检查原因，考虑撤回或替换方案，避免叠加补丁和穷举组合。

- 测试规模与风险相称。先验证主要路径，再覆盖相关高风险情况；没有新改动、失败或明确风险时，不重复扩大测试。测试数量、代码量、截图量和报告数量不能代替产品完成度。

- 美术与交互先完成一个在游戏中成立的完整样板，再扩展更多资产和变体。候选生成、技术检查、实际应用和人工认可分别报告；方向不合适时及时淘汰，不因已有投入继续堆积中间产物。

- 区分技术实现选择与产品决定。常规可逆实现自行处理；新增机制、改变玩法或扩大内容范围必须有明确依据。旧文档与代码相互一致，不等于相关决定已经得到用户确认。

- 汇报先回答：用户现在能完成什么、实际验证到了哪里、最严重的剩余问题是什么。技术细节作为依据附后。达到约定的交付标准就交付，不自行提高标准或扩展任务；不要把本应由开发验证发现的问题留给用户充当第一轮测试员。

以上规则用于帮助收敛工作，不应演变为每次小修都要重复执行的检查清单。

## 设定规则

- 所有超凡力量来自以太素；魔法是可测量、可维护、有代价的能量工程。
- 世界基调是近代魔法工业文明，不是中世纪奇幻或纯蒸汽朋克。
- 重要设定写入 `Worldbuilding/` 的源文件，不写入汇编或临时笔记。

## 美术规则

- 涉及美术或界面改动时，按需读 `Worldbuilding/策划案/OCC_项目总策划案_v1.0.md` 第5节“界面与表现系统”和文末“附录 A 美术规范与视觉反馈”，以及 `Worldbuilding/数据表/OCC_美术与界面规格表_v1.0.csv`。
- `OCC_项目总策划案_v1.0.md` 是唯一活动美术规范源；归档中的专项规范、历史简报、资产清单和旧 manifest 只能追溯，不得覆盖。发现冲突时先修正总策划案、规格数据表与机器镜像 `Tools/OCCArt/occ_art_contract_v1.json`，不得自行选择有利口径。
- 当前战斗 UI 基准为 1920×1080、左侧地图 75%、右侧 HUD 25%。
- 批准的直接图像生成只能产生可追溯的独立原料；原料不得直接进入 Unity。每项新美术必须先建立 `occ-art-manifest-v1`，通过 `Tools/OCCArt/validate_occ_art_asset.py`、1×／4×／灰阶／棋盘格、实际应用接触和人工审美后才可成为 `FORMAL_CANDIDATE`；完成稳定GUID与Importer／运行时复核后才是 `FORMAL`。
- 禁止使用未批准的本地生图工作台、localhost 生图API、私有relay或自动回退。唯一批准的本机生图通道是 EasyCLI `http://127.0.0.1:8317/v1`，必须通过个人 `gpt-image` 技能的 `scripts/run_easycli.py` 调用其 GPT Image 2.5 模型；用户要求修改美术时重新生成独立原料，不以程序绘图、重着色或旧原料变形代替。16px语义图、24px材质源→32px交付、32px装备／法宝／术式图标与非战场资产、64px战场交付主稿、`(W×64)×(H×64)`多格战场物件及64px单位必须按机器合同独立生产；战场透明单格物件以约48×48为主体预算，并配独立32px低分辨率伴随图供960×540使用。现有32px战场资产只作过渡，不得作为新制作基线，也不得把不同类别统一强塞进同一尺寸。

## 技能与工具维护

- 文档、AGENTS.md 或技能维护只检查差异、元数据和引用，无需启动 Unity、重新编译或进入 Play Mode。
- Funplay 插件自带的通用工作流须结合本项目解释：保存场景、Play Mode 和正式资产替换仍遵守上面的明确授权要求；通用移动端 UI 建议不改变 OCC 的 PC 像素布局和既有运行时 UI 架构。
- 个人技能按实际任务触发；飞书业务仅加载对应领域。旧 README、归档与工具中的失效路径不构成活动规则，遇到相关引用时核实当前入口。
- Unity/Funplay 任务按需读取 [UnityProject/AGENTS.md](UnityProject/AGENTS.md) 及其中列出的项目技能；本仓库根目录启动的任务也使用该入口，避免遗漏 Unity 子目录下的技能。
