# Modular ground batch: academy_courtyard_limestonefield

This batch is stopped at G0. Its three independent `surface.a` sources and their
decoder reports remain traceable in `sources/` and `qa/`. All three sources
failed the required 1.2 stable macro-lattice threshold, so no output, candidate,
contact assembly or Unity asset exists for this batch. The opaque-ground window
route did not relax the confidence threshold or create a fourth retry.
