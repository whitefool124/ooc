# Modular ground batch: academy_courtyard_sunstone

This batch is stopped at G4. Its three independent `surface.a` sources remain
traceable in `sources/`, and their decoder reports remain in `qa/`. None is a
candidate or a Unity asset.

The authorized opaque-ground route losslessly selected a centered `32x32` window
from the first stable decoded material field; its selected pixels were not resized,
interpolated, painted, or changed. It nevertheless fails human style review: the
dominant central crack and oversized stone masses do not form a calm low-contrast
tactical surface. Do not assemble these sources into a contact map.
