# 学院运行时掩体候选审阅索引

本索引对应 `AcademyBattlefieldLayoutCatalog` 的全部 20 个运行时掩体槽位。所有条目的逻辑占位均为 `[1,1]`；64px 宽或高仅为视觉画布，实装时固定以所属格底部中心锚定，不得改变 `CoverType`、选择、碰撞或地图格位。

| 状态 | 槽位数量 | 槽位 |
| --- | ---: | --- |
| `FORMAL` | 15 | 藤篮、书箱、卷轴盒、陶罐、煤斗、石花箱、折叠凳、绳圈、训练盾架、医务箱、橡木箱、试剂柜、工具柜、结界柱、封印箱 |
| 保留当前正式资源 | 5 | 工具挎包、消防桶架、铁柜、标本笼、野战讲台 |

新十项候选的 4× 场地接触汇总见 `academy_remaining_cover_candidates_4x.png`。每个候选目录内保留独立生图原料、网格 QA、原生图、1×／4×／灰阶／棋盘格／场地接触与 manifest；淘汰项仅保留原料与失败 QA，不可导入。

本批已按用户授权实装。正式资源均完成稳定 GUID、Point/Clamp、32 PPU、无 mipmap／无压缩与运行时资源加载复核；64px 画布由运行时按原生比例底部居中绘制，仍只占一个逻辑格。野战讲台经用户授权的第四次轴向重生仍未通过机器网格／画布门槛，因此未以不合格图替换现有正式资源，后续不再重试。
