# 战棋遮挡与点选可读性

主任务：VISUAL-INTEGRATION-01。分支：`codex/art-visual-iteration-20260905`。用户明确强调战棋可读性与互相遮挡后，本轮优先落实“仍能辨认单位、仍有可靠点选入口”，并保留已经生成的三张动作原料。

实现涉及 `CombatUnitOcclusion.cs`、`CombatUnitContourGraphic.cs`、`FormalBattlefieldView.cs` 与 `CombatUnitOcclusionTests.cs`。总案先登记第12.18节，再实施；本文件是证据归档。

单位原图、64px画布、世界像素倍率、脚底深度和占格不变。根据原图Alpha缓存边界，仅在原边界被后绘制单位或灯藤前沿的实际不透明像素覆盖时，用一世界像素宽的冷青/封存红补轮廓。不会把整个身体填成色块，不给未呈现单位补画，不显示AI内部目标。辅助网格在单位层顶部、信息层下方，继承RectMask2D；raycastTarget=false。

生命/护盾条保持原布局，首次出现单位时绑定所属格的指针组件，支持悬停、左键和右键；子图形、刻度、文字和轮廓不抢输入。身体仍使用既有Alpha命中规则，透明角继续穿透，点击判定没有改成整个64px方框。

| 场景 | 被遮挡单位 | 原生轮廓像素 | 资源条入口验证 |
| --- | ---: | ---: | --- |
| 同列与斜邻、藤丛，四分辨率/缩放组合 | 3 | 59 | 每组6个，全通过 |
| 同行紧邻，1920全场 | 2 | 29 | 6个，全通过 |
| 斜邻，1920全场 | 1 | 9 | 6个，全通过 |
| 分散无遮挡，1920全场 | 0 | 0 | 6个，全通过 |

合计42个生命/护盾条入口，各执行悬停、左键、右键回调并验证所属格；真实生产GraphicRaycaster命中其根节点，拖动处理器仍在视口。回调验证临时替换为记录委托后恢复，目的是隔离接收位置，不把它冒充操作系统连续点击或战斗效果实测。

静止100次 `RefreshUnitOrder` 没有新增贴图读回、边界重建或隐藏像素网格数据变化。首个拥挤布局捕获4张不同贴图的Alpha（3单位+灯藤）；无藤布局3张。Editor内含反射、已有重排/标记逻辑的100次耗时41.4–123.3ms；这是诊断数据，不是完整帧时间或目标设备性能结论。

实际View变化采样检查：显示偏移时轮廓随身体；翻转UV后刷新；源图透明后隐藏；单位消失不留残影；去掉灯藤前沿后隐藏边界59→28，恢复后回到59。正常视口前后差异全部在战场内部。临时窄视口使轮廓改变像素从112减至80，视口外改变像素为0；该窄视口只为审核，没有写入运行布局。

代码相关测试18/18通过，其中新增10项；最终全量EditMode826/826通过，用时33.11秒。初次真实Canvas渲染暴露新Graphic缺少CanvasRenderer，已增加明确组件依赖并补充带RectMask2D的Canvas渲染检查；最终编译无错误/警告、Console错误缓存为空。第一次筛选检查在编译未完成时只找到0项，未计入有效通过结果。

证据包括 `capture-metadata.json`、`transitions.json`、`clip-result.json`、`clip-narrow-result.json`、`image-diff-and-assets.json`、`focused-tests-final.json`、`full-tests.json` 和 `review-board.png`。`RenderAudit.cs` 运行后可重现七组中的默认960放大案例；替换分辨率/缩放及三个单位初始位置重现其余案例。`RenderClip.cs` 的对照关闭辅助时，须从临时Scene所有根节点查找轮廓组件，不能只查Bootstrap宿主子树；窄视口额外把viewportRect宽设为160参考像素。

人工复核结论：边缘提示补充了遮挡处的辨识信息，资源条提供了稳定入口；旧版寻迹兽横向61px和纵向角色交叠仍需通过角色构图、动作和全场节奏继续改善，不能据此宣布审美验收或“遮挡完全消除”。下一步回到已认可紧凑站姿的配套动作，并检查每个动作峰值下的单位、地形、范围及操作入口。动态实机、用户最终审美及完整游戏流程仍未完成。

正式主角/寻迹兽/火矢PNG及meta哈希与前轮一致。未替换正式资产，未保存场景，未进入Play；v3水面和灯藤继续QA_PENDING、批准0/2。
