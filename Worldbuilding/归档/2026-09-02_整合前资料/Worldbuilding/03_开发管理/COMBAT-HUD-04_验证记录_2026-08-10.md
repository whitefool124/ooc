# COMBAT-HUD-04 验证记录（2026-08-10）

## 实现结果

- 删除常驻 `combat.target` 目标预览模块；目标与敌人详情继续由棋盘悬停和右键查看承担。
- 侧栏分为“本轮行动、英雄概况、接下来、现场记录”四层。英雄概况独立显示主手、状态、生命、护盾和以太，并在资源条旁显示当前值/上限。
- 新增 `CombatTurnTrackPresentation`：当前行动者固定首位，其余活着的单位按行动时间和稳定 ID 排序；最多显示 5 位。
- 行动轨道使用连续线、顺序编号、阵营色节点、当前行底色与“正在行动”标记，不再仅显示缩进文本名单。
- 玩家文案改为自然游戏语言，移除“有效格、不可提交、提交状态、权威、确定性快照、不写回、完整规则、真实意图、诊断、签名”等开发措辞。

## 自动化与 Funplay

- 聚焦 EditMode：7 passed，0 failed，0 skipped。
- 最终全量 EditMode：311 passed，0 failed，0 skipped。
- Funplay `request_recompile` 经域重载恢复成功；`get_compilation_errors` 为 0 error / 0 warning。
- Play Mode 读回模块：`本轮行动, 英雄概况, 行动序列模块, 现场记录模块`；行动节点 5 个；玩家禁词命中 0。
- 运行态读回示例：`阿斯特拉正在行动 / 剩余 3 行动点`、`状态 · 正常`、三项资源精确值；当前行动者不会被敌人挤出可见轨道。
- Funplay Game View 捕获未包含运行时 ScreenSpace uGUI；本轮没有据此伪报视觉截图通过，改以运行态层级、矩形、文本、节点、激活态与颜色读回验证结构。
- 退出 Play Mode 后 Console error 为 0，编辑器非编译/非更新状态；`CombatPrototype.unity isDirty=false`，场景文件无 Git diff，未保存场景。
