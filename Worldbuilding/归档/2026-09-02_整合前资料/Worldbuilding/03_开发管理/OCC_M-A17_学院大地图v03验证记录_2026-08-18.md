# OCC M-A17 学院大地图 v03 验证记录（2026-08-18）

## 实际证据

- Unity 身份闸门：`Application.dataPath=E:/数据库/OCC_Codex/UnityProject/Assets`；活动场景 `Assets/Scenes/CombatPrototype.unity`；开始时 `dirty=False`、未在 Play Mode。
- 重编译：Funplay 0.6.2 外部改动导入后 `get_compilation_errors` 返回 `No compilation errors detected`。
- 聚焦 EditMode：`AcademyMap3DLayoutTests`、`AcademyMapVisualLayoutTests`、`RogueMapViewportControllerTests` 合计 `17/17` 通过；其中新断言验证 `central_library` 位于 `(768,432)`、为最大／最高地标，且 40 锚点均不在图书馆加 40 单位安全带内。
- 全量 EditMode：`549/549` 通过。PlayMode：`1/1` 通过。
- 合法运行路径：启动界面点击进入 → “近战热压” → 覆盖确认“覆盖并新开” → 学院地图；未使用反射跳转或场景保存。
- 真实 Game View 截图：`UnityProject/Temp/OCC_M-A17_v03_1920x1080.png` 与 `UnityProject/Temp/OCC_M-A17_v03_960x540.png`。两者均显示高分辨率蓝白硬表面，中央图书馆圆厅／阅览主厅是空间中心；节点、路线与状态覆盖层未烘焙在模型中并与同一相机投影对齐。

## 结论与风险

- v03 已替代 v01/v02 为建模控制基准；旧图只保留审计价值。
- 视觉复核确认无 Point 放大、像素化或体素感；960×540 下仍可读出中央地标、道路、封存塔和节点层。截图中的独立像素 UI 图标不属于 3D 地图材质合同。
- 本轮未改动 40 节点的权威拓扑、内容／阶段／许可规则或存档，也未保存场景。风险仅为后续新模块增量时必须持续执行节点安全带和同相机投影测试。
