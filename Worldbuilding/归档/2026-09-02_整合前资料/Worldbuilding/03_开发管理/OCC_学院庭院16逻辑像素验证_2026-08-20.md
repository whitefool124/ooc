# OCC 学院庭院 16 逻辑像素验证（2026-08-20）

## 结论

庭院 A–D 已由 32 逻辑像素细节稿改为独立手绘 `16×16` 逻辑像素稿，并以最近邻 `2×` 交付至原有 `32×32` 正式 PNG。运行时格子尺寸、PPU、GUID、映射与边界渲染方式均未改变。

## 美术与自动 QA

- 源稿：`Worldbuilding/05_美术与音频/正式美术生产/M-A18/source/terrain_rework_v03_logical16/academy_courtyard_a-d.pxart`。
- 交付：四张均为 `32×32` RGBA、全不透明、6 色、独立哈希。
- 像素密度：四张的每个 `2×2` 物理像素块均严格同色，自动字段 `exact_16x16_nearest_2x = true`。
- 家族平均明度：A `133.42`、B `132.87`、C `132.99`、D `133.03`；最大差 `0.55`。
- QA：`QA/terrain_rework_v03_logical16_family/academy_courtyard_abcd_contact_4x.png` 与 `academy_courtyard_mixed_12x9_2x.png`。
- 边界继续由 16×16 源稿中的石材灰缝与倒角形成；没有运行时 Outline、矢量线或独立网格层。

## Unity 验证

- `Application.dataPath = E:/数据库/OCC_Codex/UnityProject/Assets`；活动场景未进入 Play Mode。
- 正式路径仍为 `Assets/Game/Resources/Art/FormalAcademyCombat32/academy_courtyard_a-d.png`，`.meta`／GUID 未替换。
- Importer：Sprite、Point、Clamp、Uncompressed、无 mipmap。
- 编译 `0 error / 0 warning`；EditMode `580/580 passed`；Console 无 error；`0 dirty scene`。
- 未修改地图拓扑、碰撞、输入、数值、存档或运行时代码，未保存场景。

## 回退与下一步

覆盖前的 32 逻辑像素正式稿保存在 `rejected/terrain_runtime_32logical_before_v03/`。下一步只需在真实战斗画面判断粗地板是否与 64×64 单位、范围覆盖层形成正确主次，再决定是否推广到其他纯地板族。
