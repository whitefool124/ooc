# COMBAT-FLOW-01 验证记录（2026-08-11）

## 验收结论

PASS。敌方回合已由连续帧瞬时结算改为逐单位、逐动作串行演出；当前敌人会先被聚焦并显示公开意图，随后只结算一次既有权威计划，结果停留结束后才允许下一名敌人行动。

本任务仅改变表现节奏和反馈，没有修改敌人 AI、命令选择、AP、魔力、技能、伤害、行动顺序、公开意图或存档语义。

## 实施合同

- 聚焦：0.65 秒，状态不结算。
- 结果停留：移动 0.70 秒、攻击 0.90 秒、技能 1.00 秒、其他 0.75 秒。
- 行动间隔：0.30 秒。
- 同一时刻最多存在一个运行中的敌方行动流程。
- 每个流程只发出一次结算、结束行动和进入下一位信号。
- 展示层只消费 `EnemyIntentPresentation`；执行命令只由 `EnemyTurnPlanBook` 提供。

## 自动化验证

- 新增 `EnemyTurnSequenceTests` 5 项：聚焦等待、单次结算/结束/间隔、动作时长排序、并发行动者拒绝、重置隔离。
- 最终全量 EditMode：316 passed / 0 failed / 0 skipped，耗时 5.79 秒。
- Funplay 测试任务：`a7c81a56-569a-46d8-a7c5-1a62d3d324c1`。
- Funplay 重编译：0 error / 0 warning。

## Play Mode 运行态验证

用户已明确授权进入 Play Mode。通过 Funplay v0.6.0 在正式战斗初始化后结束英雄行动，并读取首名敌人的运行态表现：

```text
feedback=True
active=enemy_0
phase=Focus
focus=enemy_0
banner=True
name=敌方行动提示
text=敌方行动 · 铭盾卫 | 移动 → (7,2)
```

运行过程中观察到 `enemy_0` 完成后才由下一行动者进入 `Focus`，未出现整轮敌人同帧全部结算。时序精确性由纯状态机测试覆盖，运行态读回用于确认协调器与正式提示层已实际接通。

## 退出态与资产检查

- Unity Editor：非 Play Mode、非暂停、非编译、非更新。
- Console：无 error。
- 编译：无 error 或 warning。
- 场景：0 dirty scene。
- 未保存场景，未修改 `.unity`，未新增或替换位图资产。
