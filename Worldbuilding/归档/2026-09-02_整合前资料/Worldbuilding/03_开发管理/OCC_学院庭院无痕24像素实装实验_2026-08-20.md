# OCC 学院庭院无痕 24 像素实装实验（2026-08-20）

## 目的与状态

按玩家要求，将当前学院庭院 A–D 的石面污渍、矿物斑、磨痕和旧修补全部删除，并尝试原生 `24×24` 实装密度。该版本已覆盖正式稳定路径供画面审查，但仍标记 `RUNTIME_EXPERIMENT`：现行美术规范与自动测试继续锁定 `32×32` 地块，未在玩家确认前擅自放宽。

## 资产结果

- 四张源稿位于 `正式美术生产/M-A18/source/terrain_runtime_clean24_v06/`，均为独立手绘 `24×24` 文本像素网格。
- 四张 PNG 位于 `normalized/terrain_runtime_clean24_v06/`，当前已覆盖 `UnityProject/Assets/Game/Resources/Art/FormalAcademyCombat32/academy_courtyard_[a-d].png`；文件名、`.meta`、GUID 和运行时映射保持不变。
- 石面主体均只有 `1` 个底色；没有亮斑、棕斑、划痕、矿脉或内部图案。A–D 的差异只存在于下沿／右沿三像素范围内的深色接缝缺口。
- 四张均为 `24×24`、Alpha 仅 `255`、各 3 色、四个独立哈希；平均明度为 `134.97 / 134.65 / 134.90 / 134.69`，家族最大差 `0.32`。
- QA 接触表与混铺图位于 `QA/terrain_runtime_clean24_v06/family/`。上一版 v05 已归档到 `rejected/terrain_runtime_large_flags_v05_before_clean24/`，可直接回退。

## Unity 验证

- 身份门禁：`Application.dataPath = E:/数据库/OCC_Codex/UnityProject/Assets`；Editor 非 Play Mode；dirty scenes `0`。
- Unity 实际读回：四张均为 `24×24`、PPU `32`、Filter `Point`；Importer 同时保持 Sprite／Clamp／Uncompressed／无 mipmap。
- 编译：`0 error / 0 warning`；Console：无 error。
- EditMode：`578 passed / 2 failed / 0 skipped`。两项失败均来自既有 `MA18AcademyCombatArtContractTests` 对 `32×32` 的明确尺寸断言：`FormalAcademyCombat32_ContainsExactlyThe48StableAssets` 与 `P0AcademyCourtyard_IsAnIndependent32PixelPointSprite`。没有功能、加载或编译失败。
- 本轮未修改上述尺寸测试、美术规范、运行时代码、PPU、地图、玩法或数值；未保存场景、未进入 Play Mode。

## 待玩家决定

1. 若真实画面认可 24×24：再把纯地板的 24×24 例外写入美术规范，并同步两项尺寸合同测试。
2. 若密度或缩放表现不理想：保持规范不变，直接从 v05 归档回退 `16×16→32×32`。
