# UI-COPY-PLAYER-70 正式玩家文案重写验证

- 日期：2026-08-29
- 归属：剧情／肉鸽共用表现层
- Unity 工程：`E:/数据库/OCC_Codex/UnityProject`
- 场景：`Assets/Scenes/CombatPrototype.unity`

## 目标

让正式游戏文字优先回答“这是什么、会发生什么、我要做什么”，删除玩家不需要理解的实现术语；按钮说明动作，失败与禁用提示同时说明原因和下一步。

## 实现结果

- 在 `OCC_UI学院档案视觉规范_v0.1.md` 增加玩家语言合同：结论先行、按钮使用“动词 + 对象”、数值必须带对象、危险结果直说、成功提示说明去向。
- 新增 `PlayerFacingCopy`，统一“不耗时／耗时与战后时间”“进入最终战”“行动点不足”“返回地图不消耗资源”等高频句式。
- 重写正式入口、学院地图、节点房间、合并行动档案、战斗 HUD、背包、奖励结算、档案、存档与确认／拒绝提示。
- 玩家路线改为“近战训练／均衡训练／远程训练”；正式内容中的 `AP` 改为“行动点”。
- 最终战限制直接显示还需完成的节点数与核心许可数；装备、法宝和节点选择直接显示消耗、获得与失败后果。
- 修复存储不可用时仍允许点击“继续游戏”的状态与文案不一致。

## 自动化验证

- `PlayerFacingCopyTests`：4/4 通过。
- 全量 EditMode：670/670 通过，失败 0、跳过 0。
- Unity 编译：错误 0、警告 0。
- Unity Console：error 0。
- 编辑器：未进入 Play Mode，未暂停，未编译。
- 场景：`CombatPrototype` 已加载且 `isDirty=false`；未保存场景。
- `git diff --check`：通过，仅存在仓库既有的 LF/CRLF 提示。

## 边界

- 本任务未改玩法数值、地图拓扑、内容 ID、输入、存档结构或正式美术资产。
- 开发者控制台和训练靶场中的调试标签不属于正式玩家路径，未纳入本轮玩家文案合同。
