# OCC 肉鸽界面正式像素资产验证记录（2026-08-17）

## 1. 范围

- 任务：`ROG-UI-ARTPASS-01` M-A11／M-A12 正式资产化。
- 模式：仅 `rogue11` 的地图、战前简报、战外整备、战斗 HUD／背包表现层。
- 不涉及：伤害、装备规则、货币规则、护盾、阶段结构、存档语义和场景保存。

## 2. 正式资产

- `FormalResourceIcons32`：金币、阶段贡献、公开时序、已探索、核心许可、风险、重量、以太负荷、剩余次数，共 9 张原生 `32×32`。
- `FormalEquipmentSlotIcons32`：主手、副手、头部、胸甲、手部、腿部、背架、以太核心、导器、饰品一、饰品二，共 11 张原生 `32×32`。
- `FormalMapStateIcons16`：当前位置、可前往、已清理、已访问、权限不足、已知、未知，共 7 张独立 `16×16` 微图标。
- 全部图标使用硬 Alpha、近黑轮廓、平涂有限色板；不含文字、渐变、抗锯齿、环境光、透视、Emoji 或 Unicode 临时符号。

资产简报、4× 审查表和机器报告位于 `Worldbuilding/05_美术与音频/正式美术生产/M-A11/`；报告为 `27/27 QA_PASS`。

M-A12 新增 `FormalAcademyEquipmentIcons32` 与 `FormalAcademyEquipmentFootprints`：32 件学院装备各有一张独立 32×32 内容图标和一张严格匹配运行时占格的背包素材，共 64 张。占格素材在独立逻辑画布绘制，不拉伸内容图标；机器报告为 `32 件 / 64 资产 / 64 QA_PASS`，简报与审查表位于 `正式美术生产/M-A12/`。

## 3. 正式界面接线

- `FormalArtRegistry` 增加资源指标、装备位和地图状态三个严格域；未知 ID 继续抛错，不允许回退到通用占位图。
- 地图状态栏用正式图标配合短值显示生命、魔力、金币、贡献、时序、探索和许可。
- 地图节点移除“现／可／清／锁／？”字符前缀；状态由 16px 微图标、按钮色和选中焦点共同表达；路径仅使用线宽和语义色，不再叠加文字。
- 战前风险／时序／恢复指标改用专属资源图标，不再借用高风险覆盖层或行动点图标。
- 战外整备与战斗背包统一使用 11 个装备位图标；物品详情的占格、重量、以太负荷、行动点和次数改为图标化指标。
- 空装备槽继续显示 M-A11 槽位语义图；已装备槽、选中详情和奖励卡显示 M-A12 逐件内容图标。
- 战外整备与战斗背包的装备物体统一显示 M-A12 占格图；旋转仅旋转当前物体素材，不创建第二个显示对象。
- `FormalArtRegistry` 对 32 个装备稳定 ID 同时提供严格内容图标路径和严格占格路径，未知 ID 抛错且无通用回退。

## 4. 验证证据

- 工程身份：`Application.dataPath=E:/数据库/OCC_Codex/UnityProject/Assets`；活动场景 `Assets/Scenes/CombatPrototype.unity`。
- Unity 导入：Sprite／Single、Point、Clamp、无 Mipmap、无压缩；32px 资产 PPU32，地图状态微图标 PPU16。
- Funplay 重编译：`0 error / 0 warning`。
- M-A12 聚焦 EditMode：`28/28 passed`。
- 全量 EditMode：`529/529 passed`。
- Console：无 error。
- 场景：`isDirty=false`，未保存场景，未进入 Play Mode。

### M-A12 最终显示补充

- 用户授权后进入 Play Mode，运行态从启动页继续到 `rogue11` 地图并打开战外整备。
- `1920×1080`：地图状态、40 节点、详情、整备 11 槽、6×10 背包、逐件内容图标和占格图均无裁切或越界。
- `960×540`：主标题、槽位、背包网格、物品形体与操作按钮保持可辨；逐件占格图使用整数像素簇，无非等比拉伸和第二物体重影。
- 截图：`UnityProject/Temp/OCC_MA12_1920x1080_map.png`、`OCC_MA12_1920x1080_loadout.png`、`OCC_MA12_960x540_loadout.png`。
- PlayMode 回归：`1/1 passed`；退出后 Console 无 error，活动场景未脏且未保存。

## 5. M-A14 学院大地图正式资产

- 新增 `FormalMapBoard` 1 张 `670×393` 底板、`FormalMapNodeFrames77x39` 7 张节点状态框、`FormalMapRegionIcons32` 6 张区域徽记与 `FormalMapRoute8` 1 张路线转接件，共 `15/15 QA_PASS`。
- 大地图从通用按钮矩阵改为学院以太网络测绘板：六个区域用低对比材质区块区分，动态路线继续位于节点下方，路线折点使用正式转接件。
- 40 个节点保持原坐标、连接、状态与权威点击逻辑；未知节点只显示凹陷未知板与图标，不再常驻重复“未侦测”文本。
- 已知节点继续显示节点类型图标、名称与短成本；节点详情增加按位置确定的六区徽记与区域名。六区映射是纯表现层，不改变拓扑或节点内容。
- `FormalArtRegistry` 新增节点框、区域和地图装饰三个严格域；15 张资产都通过尺寸、Sprite／Point／Clamp、无 Mipmap、PPU32 门禁。
- 运行截图：`UnityProject/Temp/OCC_MA14_1920x1080_map.png`、`UnityProject/Temp/OCC_MA14_960x540_map.png`；离线审查表与机器报告位于 `正式美术生产/M-A14/`。
- Funplay 证据：主工程路径正确；编译 0 错误；全量 EditMode `531/531 passed`（重复复跑一致）、PlayMode `1/1 passed`；Console 无 error；退出后 `isDirty=false`、未保存场景。

## 6. M-A15 沿海学院大地图正式替换

- 产品采用沿海／半岛学院地理；AI 构图只作为方向蓝本，未导入 Unity。正式底图由确定性像素生产脚本重新绘制为 `1600×900` 图集，30 色、硬 Alpha、Point／Clamp、无 Mipmap，且不含节点、路线、文字或奖励。
- 正式资源新增 `FormalAcademyAtlas/academy_coastal_atlas` 与 `FormalMapNodeMarkers48` 七状态节点牌；`FormalArtRegistry` 严格注册，未知 ID 抛错。正式资产总审计计数更新为 284。
- 40 个地理锚点与六区数量固定为 `7/7/7/6/7/6`，节点类型、名称、风险、时序、奖励和内容仍由 `rogue11` 运行时决定；地图表现层不改变权威拓扑和存档。
- 旧 `154×78` 常驻文字节点卡替换为 `96×96` 状态牌与类型图标；名称、类型、时序和摘要仅在悬浮或右侧详情出现。
- 地图视口支持左／中键拖动、10px 取消点击阈值、1×／2×离散缩放、指针中心缩放、边界夹紧、重建后视角保持、当前位置回中、六区图标定位、WASD／方向键／手柄平移。详情栏可收起并把地图扩展至全宽。
- 资产 QA：图集与 7 张节点牌全部 `QA_PASS`；审查表、40 锚点覆盖图和机器报告位于 `Worldbuilding/05_美术与音频/正式美术生产/M-A15/`。
- 运行截图：同目录 `OCC_M-A15_学院沿海大地图_1920x1080_v01.png` 与 `OCC_M-A15_学院沿海大地图_960x540_v01.png`；两档均无面板越界，图例和视口控制固定在地图层上方。
- Funplay：`Application.dataPath=E:/数据库/OCC_Codex/UnityProject/Assets`；编译 `0 error / 0 warning`；聚焦 EditMode `41/41`、全量 EditMode `542/542`、PlayMode `1/1`；运行态画布 `3200×1800`，2×边界钳制与视角恢复断言通过；Console 无 error；活动场景 `0 dirty`，未保存场景。
- M-A14 保留为产品视觉否决的技术证据，不再作为正式地图入口；M-A15 已取代其底板和大节点卡表现。

## 7. 剩余验收

1. M-A11／M-A12 的生产与技术门禁已经完成；后续只需真实笔记本操作时长与主观阅读反馈，不再阻塞正式资产状态。
2. M-A13 三张背景设定图仍只作方向探索，未旁路 M-A14 正式像素资产门禁。
3. `ROG-UI-ARTPASS-01` 继续处理其余开局／简报／战斗画面的资产化与小屏信息层级；地图入口已由下述 M-A17 3D 沙盘取代。

## 8. M-A16 终止与 M-A17 3D 学院沙盘

- M-A16 平面像素底图试产因建筑盒状、道路工程线、材质与地标层次不足被产品否决；`academy_peninsula_atlas` 未注册且运行时不加载，离线图仅作否决证据。
- M-A17 删除地图对静态底图的依赖，运行时以 13 个独立建筑参数、6 条主路、中央石庭、封存塔区、以太渠和岸线多边形构成 3D 学院沙盘。
- 专用正交相机为 55° 俯角、-18° 偏航、`7.35` 正交尺寸；输出 `768×432` Point RenderTexture。节点、路线、资源栏和详情保持独立 UGUI。
- 初次 PlayMode 截图发现节点仍沿旧二维坐标漂在岛外；已增加与 3D 相机严格一致的世界坐标投影，40 节点、路线、当前位置回中和六区定位统一修正。
- 地图状态牌由 48px 缩为 32px；状态芯片单行排布；全局路线降为 2px 低对比虚线，仅当前可达使用 4px 冷青；详情改为右下摘要卡按需展开。
- 最终证据：Funplay 0.6.2 编译 0 error / 0 warning；聚焦 EditMode 8/8；全量 EditMode 545/545；PlayMode 1/1；Console 无 error；`CombatPrototype.unity isDirty=false`、未保存场景。
- 1920×1080 与 960×540 最终运行截图位于 `正式美术生产/M-A17/`；后续只增量丰富 3D 地标轮廓，不恢复平面底图。
