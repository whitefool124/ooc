# ROG-ACADEMY-NODES-32 学院第一层节点内容验证

- 日期：2026-08-24
- 状态：`IMPLEMENTED / AUTOMATED GATE PASSED / PLAYTEST REQUIRED`
- 范围：仅肉鸽模式第一层学院阶段节点；不改入学渠道、下一人生阶段、剧情模式或美术资产。

## 实现结论

1. 保留既有 40 节点拓扑及 `18 普通战 + 6 精英 + 8 事件槽 + 4 服务 + 2 宝藏 + 起点 + 固定首领` 配比。
2. 新增 `EV01–EV16` 事件池。每局以固定种子向 8 个事件槽无重复分配，节点到事件的映射写入 `rogue11`，保存／读取不重掷；旧格式仍走旧节点目录。
3. 事件选项可实际支付／取得金币与学院贡献，可改变生命／个人魔力，并可发放具体术式、装备、法宝或核心许可。需要战斗的选项在胜利后才应用唯一奖励。
4. 4 个服务节点使用学院金币／贡献，零时序、单节点只结算一次；工坊免费整理只能领取一次，不形成资源循环。
5. 两处宝藏分工明确：前置器材库在冒险封签与险地冷凝器中二选一，不含许可；封存管理员匣在冒险封签与核心许可中二选一。
6. 普通／精英／事件追加战均支持“胜利”和“角色存活但任务失败”。后者关闭节点并保留已付成本，只发基础金币／贡献向下取整的一半，不发事件唯一物、许可或战后奖励。
7. 地图节点详情显示本局固定事件名称，并明确“读档不重掷”；确认模型会检查货币、生命安全、唯一物重复与背包容量。

## 数值一致性复核

本段依 `occ-design-balance` 合同作非独立复核；作者与复核者相同，不能替代真人试玩或独立平衡批准。

- 基准：普通事件基础 `1 金币 + 1 贡献 / 1 时序`；事件战胜利基础 `3 金币 + 2 贡献 / 1 时序`；存活失败基础 `1 金币 + 1 贡献`，且选项收益不落账。
- 下界：免费事件分支只给有限金币／贡献，且事件槽一次性、消耗 1 时序。
- 期望档：收费分支主要在 `1–4` 金币或 `1–2` 贡献区间交换资源、治疗或普通道具；功能节点零时序但每节点只可结算一次。
- 上界：唯一物、核心许可和高价值装备主要绑定收费或追加战；战败不能携出这些上界奖励。
- 支配性检查：无可重复访问的免费产出；前置宝藏不能绕过许可路线；相同唯一物重复时必须换选项；事件战的额外收益只在胜利落账。
- 结论：实现与现行策划的风险／奖励口径一致，自动一致性门禁通过；具体手感和最优路线集中度仍为 `PLAYTEST REQUIRED`。

## 自动验证证据

- Funplay 身份：`Application.dataPath = E:/数据库/OCC_Codex/UnityProject/Assets`。
- 场景：`Assets/Scenes/CombatPrototype.unity`，未进入 Play Mode，未保存，结束时 `dirty=false`。
- 编译：`0 error / 0 warning`。
- 聚焦 EditMode：`73/73`，job `ee0e92c5-0f09-4c2f-818a-6f9ced845673`。
- 全量 EditMode：`619/619`，job `a10639c8-db48-4a1a-ad17-a1427263288b`。
- Console：无 Error。
- 覆盖项：40 节点配比、16 事件定义、64 组固定种子无重复分配、Rogue11 往返、货币扣除、一次性结算、事件战胜／败奖励、两类宝藏、旧节点选项兼容、遭遇目录和战斗结算协调。

## 授权后真人试玩清单

按项目规则，本任务未自行进入 Play Mode。授权后使用固定种子 `240824`：

1. 从起点依次检查补给点、工坊、医务室，确认价格、不可重复领取和零时序。
2. 连续进入两个事件槽，核对事件名称、选项成本／收益、战斗目标、敌群摘要与失败档；保存读取一次，确认事件不变化。
3. 各完成一次事件战胜利与角色存活失败，核对失败节点关闭、基础半收益和唯一物／许可不发放。
4. 到前置器材库确认仅有两种法宝；到封存管理员匣确认法宝／许可互斥。
5. 在 `1920×1080` 与 `960×540` 检查节点详情、按钮不可用原因和长事件名是否裁切。

下一步只根据这次试玩的路线集中度、首领前金币／贡献／许可分布调整数值，不扩展本任务范围。
