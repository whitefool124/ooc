# OCC M-A37 学院门楼返修候选生产简报 v0.1

> 状态：`1/1 REVIEW_READY / PRODUCT REVIEW PENDING / NON-UNITY`，2026-08-30。  
> 数量：`academy_gate_3x2` 非 Unity 返修候选 1 件。

## 返修依据

- M-A18 原 `academy_gate_3x2` 因门柱视觉轮廓与永久阻挡占格不一致被拒绝部署；源稿与 `PROTOTYPE` manifest 保持原状。
- 产品已批准 M-A32 T03 的返修方法：3×2 画布，中间一列两格明确可通行，左右两列承重体明确阻挡。
- 本批只生产独立替换候选，不覆盖旧资产、不复用旧 GUID、不导入 Unity。

## 画面与占格合同

- 原生 `96×64`、固定朝向、左上光；浅暖石灰石、深灰褐阴影、少量老木与锻铁，5–12 色。
- 左侧承重墩严格收在左侧 `32×64` 列，右侧承重墩严格收在右侧 `32×64` 列；两侧可以有低矮基脚，但不得侵入中央列。
- 中央 `32×64` 列从前缘到后缘保持连续可读通道；上方门楣只作为高架轮廓，不在地面形成第三个阻挡体。
- 禁止烘焙格线、正面立视、铁路闸门、科幻门、密集齿轮、发光护盾、文字、Logo、柔光与黑底。

## 状态边界

机器 PASS 与占格接触只使本件达到产品评审状态。未经过产品审美、稳定 GUID、Importer 与真实运行时阻挡复核，不得成为 `FORMAL_CANDIDATE` 或 `FORMAL`。
