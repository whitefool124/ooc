# OCC V2-07 主角四帧待机动画审查

- 审查日期：2026-07-25
- 范围：主角 `idle` 四帧独立原料与规范化输出；不包含攻击、受击、死亡或其他兵种动画。
- 输入：`raw_frames/frame_00.png` 至 `frame_03.png`。每张均由同一已批准主角静帧作为身份参考单独生成，未从拼板硬切。
- 输出：`frames/`、`strip.png`（`256×64`）、`preview.gif`、`qa_4x.png`、`palette_4x.png`、`report.json`。

| 检查项 | 证据 | 结果 |
| --- | --- | --- |
| 独立帧 | `report.json` 记录 4 个独立输入 | PASS |
| 固定 cell | 4 帧均为 `64×64`，strip 为 `256×64` | PASS |
| 中心/脚底对齐 | 四帧标准化边界均在 `Y=12..58`，中心线位于 `X=32`；见 `qa_4x.png` | PASS |
| 透明边界 | 统一色键清理、硬 alpha 输出 | PASS |
| 调色板 | 每帧均量化为 24 色；见 `palette_4x.png` | PASS |
| 表面语义 | 四帧保留蓝色工业战术外套、长枪与站立战备轮廓，构成低幅度待机循环 | PASS |

## 审查结论

按当前“规格符合、表面含义可读”的批准标准，`occ_hero_idle_4f` 升为原料库 `FORMAL`，允许导入 Unity 并以固定循环替换主角静帧绘制。此批仅批准待机循环；不能作为攻击或受击动画使用。
