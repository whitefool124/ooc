# 核心许可概念复查与清理

日期：2026-09-06。所属系统：肉鸽模式的学院地图、奖励、界面与存档。

目标：按用户要求全盘清除“核心许可”及同义“权限卡”作为资源、奖励与通行门槛的概念，保留旧存档进度。

验收：飞书母版与本地镜像一致；活动玩法、数据表、界面、资产与生成工具不再使用该概念；完成节点数决定提前终考资格；历史存档可以迁移；Unity 编译、相关测试和实际界面检查通过。

完成后解锁的下一步：返回原有 VISUAL-INTEGRATION-01 动态整体流程与角色、材质、特效验证；本轮没有扩展玩法内容规模。

## 来源

Git 可追溯的引入点是 2026-08-14 16:35:36 +0800 的提交 `f9d1bdb`：`docs(roguelite): define academy first-region contract`。早期学院首区方案将“完成 12 个节点 + 2 张核心许可”设为提前首领条件，并把许可放进精英奖励；随后 `e027a86`、`dadd14b` 等实现继续加入奖励和门槛。该旧口径后来进入现行总案与界面。

上述证据解释了概念如何进入工程；提交作者身份本身不构成用户对这项玩法的明确确认依据。

## 完成内容

- 飞书编辑母版修订号由 369 更新到 373，修改资源用途、提前首领条件、地图顶栏、首次精英奖励四处；重新读取全文同步本地总策划案，并核对一致。
- 删除核心许可计数、获取与发放逻辑、地图通行要求、首领附加要求，以及相关奖励文案。普通节点按连通关系开放；提前终考要求完成 12 个节点，阶段时间 28 的既有终考规则保留。
- 地图顶栏改为生命、魔力、金币、学院贡献、阶段进度、已完成节点六项，重新分配宽度。计数使用已完成节点而非已到访节点。首次体验仍按自身 10 节点及精英前置规则显示。
- 原“许可档案”改为“档案整理”，事件选项及奖励说明同步清理。事件战斗展示既有金币和学院贡献收益；首次精英未冻结的奖励内容不再绑定许可。
- 同步核心规则与学院节点内容数据表、开局体验工作稿、决策工具、地图预览脚本、语义图标生成脚本和资源图标准备脚本。
- 备份并删除两处专用许可图标及其 meta，移除注册项；旧生产原料与过期混合验证报告进入本目录 `retired-art/`。未用新美术替换任何正式资产。
- 新增独立存档迁移边界：识别历史节点及选项名称，移除已知旧奖励标记，丢弃历史计数。保留金币、贡献、种子、已完成进度和已保存内容分配；迁移存档保留原始备份。未知奖励标记仍报无效，避免吞掉损坏数据。

本轮文件清单及 SHA-256 见 `final-changed-files.json`，共 36 个新增、修改或删除的文件（含 meta），不含过程归档与已归档原料。`task-only.diff` 以本轮开始前的逐文件备份为基准，避免混入工作区此前尚未提交的改动。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 相关地图、奖励、存档、文案与美术 EditMode 回归 | 174/174 通过；任务 `ef14cac3-5f2b-4684-8037-abb904025f95`，17.45 秒 |
| 补齐旧节点名称的原始存档校验后，存档与迁移专项复测 | 36/36 通过；任务 `2a6ec797-0bd4-47da-a106-1c346a43c848`，1.26 秒；见 `final-save-tests.json` |
| Unity 最后编译 | 0 错误、0 警告 |
| Unity Console 最后检查 | 无 error |
| 实际地图界面 | 六项顶栏；首次体验 0/10；常驻地图完成 11/12 时提示还需 1 个地点，12/12 时可以参加终考 |
| 飞书全文与本地镜像 | 修订 373 内容一致（忽略文件末尾换行） |
| 全项目文本复查（含 ignored 文件，排除缓存与历史归档） | 旧术语只在 `AcademyMapSaveMigration.cs` 和 `AcademyMapProgressCleanupTests.cs` 中出现 |
| 地图预览脚本数据解析 | 40 节点，节点重命名后的连接有效 |
| 交付时 Unity 状态 | 主工程路径正确；Play Mode 已退出；未编译中；CombatPrototype 场景无未保存改动 |

174 项通过后追加了原始旧存档入口检查，发现旧节点名称在迁移前被拒绝；修复后执行上述 36 项专项复测。两次通过数不是互不重叠的总测试数，也不代表整个仓库全量测试。

`runtime/first_run.png`、`runtime/eleven_completed.png`、`runtime/twelve_completed.png` 是 Play Mode 中临时构造的对应地图状态截图，用于界面与门槛验证；不代表人工从头完成整轮游戏。没有写入玩家存档，也没有保存场景。

## 有意保留的历史内容

`Worldbuilding/归档/` 的旧设计、此次修改前备份以及退休原料保留原貌，用于追溯。活动代码中的历史字符串仅用于读取旧存档和回归验证，不再是可获得资源或生效门槛。旧存档报告中的 `access_cards` 是被丢弃字段的历史标识。

## 另外发现的既有冲突

`Worldbuilding/数据表/OCC_学院节点内容数据表_v1.0.csv` 的 T01、T02 行将事件耗时写为 0，而总案事件耗时口径和当前实现为 1。已明确告知用户并在此记录；本次删除许可没有擅自更改这项独立玩法决定。后续统一该数据前，仍需按项目规则确定口径。

本轮未提交或推送 Git；此前工作区改动保留。
