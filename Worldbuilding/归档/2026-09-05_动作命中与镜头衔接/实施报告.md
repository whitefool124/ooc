# 动作命中、显示快照与镜头衔接

主任务 VISUAL-INTEGRATION-01 继续进行，第12.9节尚未全部验收。主工作树 `E:/数据库/OCC_Codex`，分支 `codex/art-visual-iteration-20260905`。

## 已落地

已接受命令即时结算，表现层保存独立的显示快照。普通攻击在已存在的移动结束后出手，约160ms接触；普通技能投递180ms；正式火术逐格读取 FireVfxSequence 的真实命中节点。武器附着等触发在武器接触点启动，不提前命中。命中时才更新战场血盾、状态、死亡、地形与火场显示，并发布飘字；AP和费用保持即时。右侧英雄资源栏使用同源显示快照，并在命中节点刷新。伤害扫描缓存同步已结算状态，避免在等待投射时重复发布结果。

非移动动作结束前暂缓下一次战斗输入、AI推进与结果页；连续移动规则保持。最后一个已知反馈节点后保留420ms供阅读，完整能力链更长时等链尾。关闭动画会一次性发布已结算的可读反馈并释放显示快照；重开/退出取消旧待播内容。卡顿帧按原定时间派发效果，过期VFX不会从第一帧重新出现。

放大档镜头使用当前视觉格坐标，在安全边缘仅做必要平移；默认全场档不跟着移动，避免结算后镜头提前跳到终点。修正了上下方向局部攻击偏移的坐标符号。

普通攻击、火术、法宝和靶场入口均接入动作捕获生命周期。法宝复合反馈语义仍有已证实问题，见下节；不能据普通攻击测试宣称法宝全量表现正确。

## 验证及修复

- 定向回归28/28通过，job `75df4165-b57c-47c3-988a-d1375fa30639`，1.69s。
- 全量EditMode **775/775**，失败0、跳过0，24.41s，job `bde04288-88bf-4827-b639-959c6d51f5bc`。新增11个用例覆盖致命伤显示与动作费用、显式多段伤害去重、真实火弹、武器触发、地形与火场快照、动画关闭、迟到帧、反馈缓存、两档镜头与自定义上限复制。
- 首次定向测试发现命中边界上飘字已发出但死亡快照仍存活。独立Unity探针复现：真实HP=0，0.5s显示HP=12，0.500001s显示HP=0，同时已派发2项反馈。原因是回调保存的单精度时刻与快照现场加法的中间精度不一致；现统一存储每格唯一命中时间，回归通过。
- 复核复制入口发现 `UnitState.Clone()` 遗漏 `MaxHealth` 和 `MaxMana`。已在复制函数本身修复；测试用87生命上限和20魔力上限确认复制、恢复封顶与副本独立性。该修复保持配置数值，不作数值平衡调整。
- 最终Unity编译0 error / 0 warning，Console缓存无error，美术合同审计PASS。表现代码diff空白检查通过。
- Unity路径为 `E:/数据库/OCC_Codex/UnityProject/Assets`，CombatPrototype未改脏。未进入Play Mode，未保存场景、未替换正式资产。Editor性能快照只作当前编辑器状态记录，不构成游戏运行性能证明。

## 已证实未完成项与下一步

法宝 `ArtifactStep.Applied` 不总是“目标生命损失/护盾增加”：Damage保存生命与护盾合计减少，TransferShield保存双方护盾差值的缩小。旧 `NotifyArtifact` 分别把它直接当Damage和ShieldRestore，可能将护盾再次计入总伤害，或显示不存在的护盾增加。只调整时序不能修复含义。

只读内存探针已执行G-T16护盾均衡阀：主角护盾2→4、友军6→4，返回 `0:TransferShield:ally:(1, 0):4:Always`；旧映射会在实际减少2护盾的友军上展示增加4。下一步必须保留逐项真实资源变化及受影响单位身份，区分承伤、消耗与转移，并验证反噬归属和多状态施加。该复合反馈问题已写回母版当前项，未标记第12.9节完成。

随后仍需双分辨率实机验证动作节奏、点击体验、单位拥挤、灯藤遮挡、完整流程与性能。原有Play Mode授权问题尚未获答，本轮继续了不依赖该授权的实现与EditMode验证。水面/灯藤v3仍QA_PENDING，当前人工批准0/2；B2/B3/精英/奖励等未冻结内容不由本轮临时决定。整体完整游戏目标保持进行中。

## 文件与同步

新文件：CombatActionPlayback、CombatUnitDisplaySnapshot及其测试。修改反馈系统、Bootstrap、战场Presenter、HUD、移动时钟查询、视口和UnitState复制函数。修改前文件在 `before/`；第12.9节先入飞书母版后实施，规格表与机器镜像同步。验证原始返回单独归档，最终母版全文同步本地；未自动提交或推送。
