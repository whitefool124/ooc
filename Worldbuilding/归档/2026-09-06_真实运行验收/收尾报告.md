# VISUAL-INTEGRATION-01 真实运行验收

日期：2026-09-06。用户已明确授权进入 Play Mode，并要求快速收尾汇报。

目标：在主工程验证已完成的战场视觉整合，重点检查移动、攻击命中、反馈清理和血条点选。涉及系统为 CombatPrototypeBootstrap、CombatVisualFeedback、FormalBattlefieldView。本轮没有新增产品代码改动。

验收标准：真实输入可到达生产战斗流程；移动出现连续中间位置；命中前后表现生命值按时切换；动作结束后反馈对象消失、操作锁解除；血条点选归属正确；退出后场景无脏标记、Console 无运行错误。

## 已验证

- 主工程路径为 `E:/数据库/OCC_Codex/UnityProject/Assets`，场景为 `Assets/Scenes/CombatPrototype.unity`。
- 实际 UI 双击格子完成 `(1,4)→(4,4)`，行动点 `3→2`；随后完成 `(4,4)→(6,4)`。逐帧采到 x 坐标 `4.000, 4.381, 4.937, 5.298, 5.641, 5.991, 6.000`，y 恒为 4。动作最终不再占用操作锁。
- 为构造近距离射击样本，仅在本次 Play 内临时给主角装备已有步枪；随后通过 UI 点击敌人格子执行生产攻击流程，没有直接改写伤害结果。
- 第 13755 帧：逻辑/表现生命 `12/12`，无反馈；第 13765 帧：`9/12`，动作锁开启；第 13771 帧：`9/9`，1 个伤害文字和 1 个特效出现。见 `attack_impact.png`。
- 攻击播放完毕后读回：`actionBusy=False`、`liveFeedbackText=0`、`activeVfx=0`。
- 右键点击 `(880,648)`，工具实际命中 `格子信息_8_5/生命`，生产选中目标为 `enemy_2`。
- 运行后 Console 查询无 error。已退出 Play，恢复临时后台运行设置；最终 `play=False`、`compiling=False`、`scene.dirty=False`。未保存场景、未导入或替换正式美术。

## 证据边界

运行初段因 Unity 不在前台且后台运行关闭，画面更新停滞；启用本次运行的后台更新后重新采样，以上移动和攻击逐帧结果取自正常更新阶段。`after_movement.png`、`after_attack.png` 等早期截图不能证明动画过程。

密集反馈另外尝试了 3 个状态通知加 1 个伤害通知的人工压力样本；截图时机未可靠捕获可见文字，`dense_fixture.png` 与 `dense_runtime.png` 不作为动态密集反馈验收通过证据。这部分保留此前 30/30 定向测试和静态布局审阅结论，不扩张为完整动态审美验收。

临时取证代码曾触发编译错误、严格文件写入拦截，以及读取已销毁临时对象的错误；均发生在工具取证片段，未作为产品故障处理。未关闭安全检查。归档通过工作区文件工具完成。

## 本轮交付与下一步

本轮完成真实运行的移动、射击、命中延迟、反馈清理及血条点选验证；此前反馈布局相关 30/30 定向测试仍有效，本轮没有代码变化，不重复全量测试。

解锁下一步：用户可审阅当前战斗视觉整合效果；仍需完成密集反馈动态审美检查，以及水面、藤蔓和寻迹兽动作候选的人审与正式接入流程。整体完整游戏目标未宣称完成。
