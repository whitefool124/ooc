# 首轮视觉打磨实施与复审

日期：2026-09-05。分支：`codex/art-visual-iteration-20260905`。唯一主任务：`VISUAL-INTEGRATION-01`。整体目标仍在进行；本轮是首战视觉基础实施，尚未完成实机或完整游戏验收。

## 目标与范围

让完整战场、单位、地形、资源和操作反馈遵循同一像素尺度，并消除右栏与底栏遮挡。以用户“飞书 rev320 为准、v3 重新审核”的裁决处理旧状态；随后采纳的表现实施合同已先写入飞书总案第12节，再同步本地镜像、规格表与机器合同。

涉及战场适配器、单位HUD布局、地块Presenter、战场View、反馈系统、正式战斗HUD、界面JSON、相关EditMode测试及美术规格。保留工作树中已有变更，未创建其他工作树或仓库副本，未提交、推送或保存场景。

## 已落实

| 系统 | 最终变化 | 用途 |
|---|---|---|
| 战场视口 | 参考安全区(16,80,1408,768)，默认64px格、全场768×576；放大128px格；全场按钮重置 | 12×9战场默认完整显示，完整显示时停止边缘自动平移 |
| 单位 | 64px单位画布按逻辑格2倍显示，脚底锚点保留，位置按2px参考量化 | 单位与32px地面使用相同世界像素倍率 |
| 单位资源 | 总览血条56×8、护盾56×4；依据高度调整内部填充 | 避免半分辨率下护盾被上下留白完全吞掉 |
| 地形与范围 | 只显示当前移动/攻击范围；取消水/藤逐格常驻名；单位悬停包括脚下特殊地形 | 降低重复标识，同时保留规则可访问性 |
| 特效 | 原生32px帧按当前64/128格整数绘制；画布与主UI缩放方式一致并启用pixelPerfect | 避免58px任意拉伸和非等比例窗口中的层间错位 |
| 右侧HUD | 448×768，底边848，与底栏顶864隔16；四模块96/244/264/96；2px细框 | 保留五个行动位、三资源与两行摘要，消除底部记录遮挡 |

右栏保留模块射线接收以支持悬停详情。资源标签为32px高，轨道20px高，三行不会互相覆盖；行动点位置改为偶数参考坐标。没有改生命、伤害、AP、地形效果、AI、遭遇或存档规则。

## 验证

| 检查 | 实际结果 |
|---|---|
| Unity目标 | Application.dataPath为主工作树Assets；CombatPrototype场景；Play Mode关闭；场景未脏 |
| 编译 | 域重载恢复记录成功，编译0错误、0警告 |
| 首批布局/悬停测试 | 84/84通过 |
| HUD与视觉合同定向测试 | 最终62/62通过 |
| 最终全量EditMode | 740/740通过，0失败/跳过，23.09秒；job cb14bf05-9576-4377-89d3-72c32832af1b |
| 机器美术合同 | PASS；采用总案第12节后，旧“2px细框”缺口已由正式规格补齐，未放宽验证器 |
| v3技术复核 | 2/2 PASS；仅在内存以QA_PENDING复核，未改写历史manifest或晋级 |
| GUID / Importer | 两项稳定GUID与历史一致；Sprite、Point、Clamp、32PPU、无压缩、无MipMap |
| 差异检查 | 本轮运行时和测试文件git diff --check通过，仅有Git行尾提示 |
| 新实机接触 | 未执行；现有截图均为此前版本，不能证明本轮的视觉效果 |

文档同步到飞书rev326后，本地镜像与抓取全文一致。补充检查发现总案第581行的源文档空列表项带尾空格，文档级`git diff --check`因此返回1；该既有格式问题保留母版原样，不影响上述代码检查或编译结果。

新增/调整测试验证两分辨率的完整格位与意图边界、缩放重置、血盾分离及可见填充、单位地形说明、右栏模块互不重叠、五个行动序列行的字体与边界，以及模块可悬停。测试结论只覆盖其实际检查范围。

## v3复审意见

当前状态仍为 **QA_PENDING，人工批准0/2**。旧manifest内的FORMAL与人工PASS是被本轮裁决撤回效力的历史记录，没有重新沿用。文件保留供追溯，现有Unity资源仅用于复审接触。

- 水面：32×32、6色、全不透明；灰蓝湿石表现安静，但表面纹理与普通石地接近。必须在移除文字标签后的总览尺度验证“五格水道”能否被直接识别；技术PASS不代表材质识别通过。
- 灯藤：32×32、8色、硬Alpha及透明边通过；中空树篱轮廓明确。单位画布扩大后，须检查人物、前缘藤叶、血盾和邻格单位的遮挡关系；不能用旧小角色截图通过新布局的接触门禁。
- 两项都已有历史1×、4×、灰阶、棋盘格、应用图。本轮重新检查了4×图和历史应用图，并完成机器门禁与Importer读回。新的应用接触和人工审美尚缺，不晋级、不扩散复用。

## 验收与下一步

验收必须补齐1920×1080、960×540的真实界面：总览/放大/复位、单位站在水面和藤内、首末排意图、血盾变动、悬停、右栏长文本、八术式底栏、移动和结算反馈。随后评估材质身份、动作表现和完整流程内容。

项目AGENTS.md要求：“不要在没有用户明确要求时保存场景、进入 Play Mode 或替换正式资产。”Play Mode与测试存档恢复的授权问题已提出，尚无答复；本轮保持EditMode，未动测试存档。实机通过前不宣称本任务完成。后续战斗2/3、精英与奖励等继续遵循总案冻结顺序，首战样板不替代完整游戏目标。

证据位于本目录`evidence/`；修改前备份位于`before/`。本目录脚本是已执行操作记录，包含一次性断言；不要把它们作为可重复迁移工具直接重跑。
