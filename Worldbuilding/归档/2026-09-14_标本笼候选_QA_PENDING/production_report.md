# 学院标本笼候选生产报告

- 运行时槽位：`academy_prop_specimen_cage`（重掩体，单格逻辑占位）。
- 第 1 轮：网格通过（置信度 1.3356），但主体需 `64×96` 画布，超过普通单格透明物件 `64×64` 审核上限。
- 第 2 轮：画布已收紧为 `64×64`，但网格置信度 1.1594，低于 1.2。
- 第 3 轮：网格置信度 1.0113，低于 1.2。

三轮已用尽；不降低网格门槛、不几何缩放、不以程序重绘修补。所有独立原料与 QA 记录均保留，仅维持现有正式资源，不生成候选 manifest 或 Unity 导入。
