# 实际移动路径与单位信息随行

主任务 VISUAL-INTEGRATION-01，继续进行。分支 `codex/art-visual-iteration-20260905`，主工作树 `E:/数据库/OCC_Codex`。本段属于共用战斗表现，不扩展玩法内容。

## 目标、改动与验收

解决旧表现只在终点附近滑动最多28px的问题。结算层已有 MovementPath，现经 Bootstrap 与 CombatFeedbackPublisher 传给独立的 CombatMovementPlayback；复制结算路线后逐段采样，不重新寻路。默认每格80ms，总长120–350ms；连续命令保留当前视觉位置、旧路线剩余拐点与新路线，剩余时长最多350ms。逻辑位置和行动点仍由原结算即时确定，播放不写入 UnitState。

角色位移与攻击/受击的局部偏移分开。BattlefieldCellPresentation 传递按64px格量化的移动位移，FormalBattlefieldView 在128px格按两倍绘制；身体和血盾、状态、意图、四向朝向共用移动位移，单位按当前足底排序。朝向为三块整数矩形构成的界面标记，使用已有界面颜色，不生成或替换美术资产。更换同格占位单位时重置血条显示缓存，防止继承上一角色的扣血拖影。

不可信、非逐格相邻、起终点不符的路线，以及结算后又被强制位移的旧终点，直接对齐最终位置；不补画未经结算证明的路径。死亡、战斗退出、重开、组件停用与关闭动画会清理路径。没有显示未来敌人搜索路线。

## 验证证据

- Unity 身份：`E:/数据库/OCC_Codex/UnityProject/Assets`；CombatPrototype 场景未改脏；未进入 Play Mode、未保存场景。
- 新增7项 EditMode 用例，覆盖真实结算绕障碍路径、连续拐点、路径快照与过期终点、64/128两档身体和HUD同位移、动画关闭/重开后不改变最终位置和AP。连同发布器的定向测试9/9通过，job `4eb8caab-9a65-45c7-9754-81122b6c3d86`，耗时0.97s；工具只保留最近任务，该定向结果据本次实际返回记录。
- 全量 EditMode **764/764**，失败0、跳过0，24.54s，job `85cb0bf4-456f-409c-b9a5-4b232e1827a9`。
- 最终编译0 error / 0 warning；Console缓存无error；美术合同审计PASS；表现代码diff空白检查通过（Git提示工作区换行转换，不是代码错误）。

## 尚未验收与下一步

本轮是代码与EditMode门禁通过，不是运行时或审美验收。角色经过灯藤、贴邻单位、方向标记拥挤、移动时快速攻击、延后投射与即时伤害飘字的关系、128px放大档镜头跟随，以及完整流程性能，需要实际运行验证。当前普通伤害反馈仍按结算时刻发布，快速连续动作可能与移动表现重叠；下一步专门处理这些已知时序问题，不将测试通过等同于视觉统一。

水面和灯藤v3仍按用户裁决以飞书rev320为准，当前QA_PENDING、批准0/2；未作新人工批准或正式资产替换。B2/B3/精英/奖励等未冻结玩法仍需内容决定。原有Play Mode授权问题尚待回复，本轮继续了不依赖该授权的实现；整体目标保持进行中。

改动前源文件在 `before/`；总案先增第12.8节，再接入代码、规格表和机器镜像；最终状态回写母版并全文同步本地。源文件不自动提交或推送，保留原有未提交工作。
