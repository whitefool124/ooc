# 同时状态与伤害反馈避让

目标：修复同格复合反馈的状态卡完全相盖、与伤害数字重叠。属于VISUAL-INTEGRATION-01的可读性工作；不新增玩法或正式美术。

修改CombatVisualFeedback：状态卡与伤害跳字共用完整上浮轨迹预留；按81个视口内候选最小化重叠及位移，8px间距、8px安全边距、2px位置量化。保持240×48/168×104尺寸和28/52上浮、既有字号、时长、伤害合并与结算规则。空间不足仍保留反馈。移位引线使用所属单位阵营色，无单位时使用中性墨色；数字引线接近实际字形宽度，避免停在宽文字容器的空白处。引线跟随反馈淡出，浮动期间指回原格位，边缘对齐像素，不参与射线。销毁后释放位置，重开清理缓存。

相关验证最终26/26通过（新增5个测试实例），见final-tests.json，job 3ea15b14-b4c1-4a02-9ad7-7a04974da5cb。覆盖同格复合状态与伤害、动画开关下的完整轨迹、极端视口边缘与拥挤降级、确定性整数位置、销毁释放、引线实际坐标与上浮后的原点归属及非交互。此前的26/26为中间版本，最终结果以final-tests为准。最终编译无错误和警告；机器合同audit通过。本轮没有重新运行此前的全量826项。

截图：verified_960.png、verified_1920.png为最终960×540/1920×1080放大档，同格三状态加伤害；dense_960.png为邻格同时7项反馈。所有文字对象数量明确检查，没有仅保留最后一条。before.png为修改前真实捕获，使用最初审核颜色；final_before_960.png使用生产语义颜色并重放原始固定定位规则，便于与最终图比较，不能冒称它是旧代码版本截图。

静态方法：临时场景复用生产战场/HUD和反馈卡片构建代码，单独提供与生产视口匹配的临时Canvas，避免EditMode不允许的DontDestroyOnLoad。跳过DOTween在EditMode无法调度时的空引用阶段，检查四/七个实际文字对象；没有验证真实连续播放、输入、伤害结算或帧率。最终可复现脚本为RenderFeedbackVerified.cs；dense截图在该脚本额外加入邻格两状态和伤害，并将文字数量断言改为7。临时场景和纹理在finally清理，恢复此前可访问性主题设置，没有保存场景、进入Play或替换资产。早期Initialize/DontDestroyOnLoad审核失败已定位为EditMode载体限制，不作为生产运行时错误结论。

当前改善：原先只看得到最后一张状态卡，现同格与邻格结果均能分别读取，伤害不再直接覆盖状态文字。剩余：高密度反馈仍会临时覆盖部分角色，移位归属和连续淡出可读性须实机与人工审核；不宣称整个战场遮挡或最终视觉已解决。完成此轮后继续反馈密度、角色动作衔接与材质/特效整体收口。飞书母版rev363已读回并同步本地镜像，规格表与机器镜像已同步。
