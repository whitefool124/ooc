# 现有美术候选实装盘点

日期：2026-09-15  
范围：仅盘点已有原料；不生成、不重绘、不改玩法或数值。

## 已确认正式实装

以下 18 项的 manifest 均为 `FORMAL`，交付文件实际存在于主工程，且
`Tools/OCCArt/validate_occ_art_asset.py` 返回 PASS。其 manifest 均记录稳定
GUID、Point / Clamp、32 PPU、无 Mipmap、未压缩及运行时核验。战场结构资源同时
已由 `AcademyBattlefieldLayoutCatalog` 与 `CombatFormalVisualAssets` 的现有映射覆盖。

- 轻掩体：藤篮、书箱、卷轴盒、折叠凳、陶罐、煤斗、绳圈、训练盾架、石花箱、医务箱。
- 重掩体：橡木箱、试剂柜、工具柜、结界柱、封印箱。
- 战场地表：灯藤焦痕。
- 装备：`ACA-EQ-CR04` 图标和背包占格图。

这些资产已经在正式 Unity 路径中，因此本轮不复制、重导入或改写它们的 GUID。

## 跳过结论

| 候选 | 结论 | 原因 |
|---|---|---|
| 野战讲台 `field_lectern` | 跳过 | 已达三轮上限；当前 manifest 缺少交付输出与 1x 证据，验证器报 FAIL。 |
| 碎晶地表 `crystal_shard` | 跳过 | 已达三轮上限；不重新生成或重复处理。 |
| 楔角待机 `breach_ram_wedge` | 跳过 | 已达三轮上限；不重新生成或重复处理。 |
| 寻迹兽 64×32 自适应候选及其动作姿态 | 暂不实装 | 总案与机器合同存在冲突，等待用户明确裁决。 |
| 维护补给箱 `service_supply_case` | 跳过 | 技术验证可通过，但当前产品数据、战场布局和运行时资源映射均未声明其用途；引入会扩大内容范围。 |
| 维护卫士 `service_maintenance_guard` | 跳过 | 技术验证可通过，但当前敌人档案／遭遇数据未声明其用途；引入会扩大内容范围。 |
| 普通晶簇、精英稳压晶簇旧候选 | 跳过 | manifest 已标为被 2026-09-10 晶簇族取代；该旧角色不在当前机器合同中，验证器不能产生有效结果。 |
| 旧 48/64 战场、掩体和邻接实验稿 | 跳过 | 属于 `CONCEPT`／`PROTOTYPE` 或与当前原生 32 PPU、自适应画布合同冲突；不作为正式入库来源。 |
| 旧节点、开场、回合反馈及立绘 QA 原料 | 跳过 | 已有对应正式资源或仍缺少现行用途、人工审核或动画／运行时闭环；不以旧 QA 稿覆盖正式资源。 |

## 本轮验证边界

- 18 个当前 `FORMAL` 候选：manifest 机器验证 PASS；交付文件及现有资源映射静态存在。
- Unity 重试（2026-09-15）：已发现当前 Editor 的实际 MCP 端点并核对 `Application.dataPath` 为主工程；重新导入和编译均无编译错误。精确 Editor 检查确认 18 项可由 `Resources` 加载，Importer 为 Point／Clamp／32 PPU／无 Mipmap，15 个战场结构项均在 `AcademyBattlefieldLayoutCatalog` 映射中。Play Mode 下 `CombatFormalVisualAssets.LoadRuntime()` 及 5 项代表性资源读取通过。
- 相关 EditMode 测试共 55 项，52 项通过；3 项失败都在既有 UI 注册表／尺寸断言（`AcademyMapTypeIcons_AreNative48PixelSprites`、`EveryNonBlockedRegistryEntry_LoadsWithoutFallback`、`PeripheralUi_ConfigAndPixelAssetsAreComplete`），与本批 18 项结构资源无直接映射。运行时 Console 未发现错误。
- 测试场从正常选择页进入后仅渲染空背景及一个消耗标签，未形成可审美的战场接触画面，属于现有测试场表现问题；因此运行时读取通过不等同于完整人工审美复验。
- 主工作树在盘点前已包含相关 PNG、映射与大量其他未提交修改；本轮未改写这些文件，避免覆盖并行工作。
