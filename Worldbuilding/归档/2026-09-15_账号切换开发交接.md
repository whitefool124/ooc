# 2026-09-15 账号切换开发交接

## 交接目的

本记录用于在另一 OpenAI/Codex 账号中继续同一个本地工作树。它只描述本机 `E:/数据库/OCC_Codex` 在 2026-09-15 的可见状态；对话历史不作为唯一事实来源。

## 项目与约束

- 仓库根目录：`E:/数据库/OCC_Codex`；只能在主工作树开发，不建立 worktree。
- Unity 工程：`E:/数据库/OCC_Codex/UnityProject`；默认正式场景为 `Assets/Scenes/CombatPrototype.unity`。
- 当前活动产品源：`Worldbuilding/策划案/OCC_项目总策划案_v1.0.md`；飞书母版链接已在根目录 `AGENTS.md` 中。内容冲突时先报告，不自行取舍。
- 美术已明确禁用 EasyCLI/localhost/私有 relay；新图或重生图只能走 Codex 内置 `image_gen`。独立原料不等于正式资产，须走 manifest、脚本、人工审美、Unity 导入及运行时复核。
- Unity 任务先读 `UnityProject/AGENTS.md`；改脚本后通过 Funplay 重新编译并检查 Console。进入 Play Mode 已获项目级授权，但保存场景、替换正式资产仍须用户明确要求。

## 当前开发焦点

当前未提交改动集中在“学院第一阶段战斗的可玩内容与战斗测试场”。新增的 `Assets/Scenes/CombatTestArena.unity` 与下列代码组成独立测试入口：

- `Runtime/Presentation/CombatTestArenaEntry.cs`：Play Mode 显示“战斗测试场”，按普通战、精英／首领、系统验证、术式验证分组，选择后以 8 个术式槽和 4 个战术栏法宝的满配状态进入。
- `Runtime/Presentation/CombatTestArenaScenarioCatalog.cs`：已配置普通训练、精英与封存塔核心首领，以及反应、守护、效率等系统验证场景；场景文本已写入战术意图和建议路线。
- `Runtime/Combat/CombatPressureTestRuntime.cs`、`AcademyCoreBossRuntime.cs`：为高压/首领测试提供运行时行为。
- `Tests/EditMode/CombatTestArena*`、`CombatScenarioRouteHarness.cs`：已加入对应的编辑器测试文件，但本交接时点未重新执行验证，不能据此声明通过。

## 同步变更范围（均未提交）

- 战斗：`Runtime/Combat/`、`Runtime/Campaign/`、`Runtime/Grid/` 和 `Runtime/Presentation/` 有大量修改，涉及术式、法宝、敌人、目标、伤害预览、移动/地形、HUD、悬停信息、学院地图与战斗呈现。
- 内容/数据：总策划案及学院第一阶段流程、教程、核心规则、火系术式、通用法宝、美术界面等 CSV 均已改动。
- 美术：既有学院结构物、空状态插图、火术师图已被替换；另有 `FormalAcademyCombat32`、`FormalAcademyEquipment*`、角色肖像等未跟踪资源。它们不能因为位于 `Resources/Art` 就被视为正式验收。
- 工具：`Tools/OCCArt/` 有合同、校验器、标准化和批量生成辅助脚本更新/新增；`native32_generation_profiles.json` 尚未纳入版本控制。
- 归档：大量 `Worldbuilding/归档/2026-09-13_*`、`2026-09-14_*`、`2026-09-15_*` 的美术候选/QA 目录均为未跟踪资料。最新可读盘点是 `Worldbuilding/归档/2026-09-15_现有美术候选实装盘点.md`。

## 已知风险与恢复顺序

1. **不要清理、reset、checkout 或删除任何未跟踪内容。** 当前 `git status` 同时包含大量修改与未跟踪文件，先完整保留工作树。
2. 新账号打开本目录后，先读根目录 `AGENTS.md`、本记录、`UnityProject/AGENTS.md`，再运行 `git status --short`；确认工作树仍包含上述改动。
3. 用 Funplay 核对 `Application.dataPath` 必须为 `E:/数据库/OCC_Codex/UnityProject/Assets`，打开 `CombatTestArena.unity`，重新编译并检查 Console。
4. 在 Play Mode 从测试场依次验证：至少一个普通场、一个精英／首领场、一个系统验证场；优先检查是否可选场、是否能实际入战、满配术式/法宝是否可用、敌人意图和目标提示是否可读、战斗是否能正常结算。
5. 任何发现先限定在当前最严重的阻断问题；不要在本轮继续扩展美术批次或新增玩法。测试稳定后，再由用户决定是否整理提交与正式资产替换。

## 基线

- 最新提交：`0bcbc71 feat: make backpack capacity equipment-driven`。
- 本记录创建时工作树有大量未提交更改，且没有在本次交接期间执行 Unity 编译、EditMode 测试或 Play Mode 验证。
- 当前活动内容仍是“学院第一阶段”；总案写明教程为 11 个完全预设节点，之后接入约 19 个常规学院节点，教程本身不含首领战。

