# 诱导灯战场资产生产记录

- 第 1 轮：拒绝。宏像素置信度 1.11，主体约 42×86，带黑底与软光，超出 64×64 合同。
- 第 2 轮：拒绝。主体约 39×60，但宏像素置信度 1.1248，未达到 1.2。
- 第 3 轮：通过。宏像素置信度 3.291，主体 16×25，14 色，真 Alpha；无缩放、插值、抖色或新增颜色。
- 画布选择：按实际无损主体收口为 32×32、32 PPU、单格逻辑占位，底部居中；未人为放大成双格宽视觉。
- Unity 导入：Sprite、Point、Clamp、32 PPU、Uncompressed、关闭 mipmap；GUID `01ac2fe1b1741f84fbd2852a0f14f9bf`。
- 运行时：`CombatBattlefieldCellPresenter` 的 `IsDecoy` 分支加载专用贴图并显示“诱导灯”；编辑器内呈现断言通过。
- 编译：0 error、0 warning；Console 无错误。

测试运行器仍被既有卡死任务占用，因此本批没有重复提交 Test Runner，而是执行了覆盖同一映射、尺寸、标签与占格语义的编辑器内直接断言。
