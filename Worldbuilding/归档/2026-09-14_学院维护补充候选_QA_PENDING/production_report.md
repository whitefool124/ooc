# 学院维护补充候选 · 生产记录

本批仅使用 Codex 内置 image_gen；没有导入、覆盖或替换 Unity 正式资源。

| 候选 | 用途 | 结果 | 轮次 / 诊断 |
| --- | --- | --- | --- |
| 维护补给箱 | 可搜刮交互物件 | QA_PENDING，机器 PASS | 第 1 轮；网格置信度 2.6036；64×32 |
| 维护守卫 | 敌方侧视单位 | QA_PENDING，机器 PASS | 第 1 轮；网格置信度 1.2470；32×64 |
| 服务庭院地表 | 单格场地 | 拒绝 | 第 3 轮仍为不稳定主体网格（1.104 < 1.2）；停止重试 |
| 工具车 | 轻掩体 | 拒绝 | 第 3 轮不稳定主体网格（1.0665 < 1.2）且画布需求为 128×128；停止重试 |

机器验证仅覆盖两个通过候选，均为 PASS。候选尚未通过人工审美，也没有 Unity 导入、稳定 GUID、Importer 或运行时复核；状态必须保持 `QA_PENDING`。

全库 `validate_all_occ_art.py` 仍会命中归档中的四份历史不完整测试 manifest 而失败，该结果与本批单件 manifest 验证无关。
