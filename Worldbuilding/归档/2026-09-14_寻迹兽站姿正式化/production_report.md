# 缚环寻迹兽站姿候选生产记录

## 实缺依据

- 缚环寻迹兽是 FIRST-B1 的首个近战敌人，并在多组普通／强遭遇中复用。
- 历史紧凑轮廓方向已经得到用户认可；现行正式资源轮廓更散，束环身份较弱。

## 生成与筛选

- 第 1 轮：主体 46×24，尺寸适合 64×32，但网格置信度 1.001，低于 1.2，拒绝。
- 第 2 轮：网格置信度 1.2817，主体 53×28；以 23 个源色无损解码到 64×32，再按总案要求把既有最外沿像素替换为纯黑轮廓，最终共 24 色。Alpha 形状不变，无几何缩放、插值、抖色或结构补绘。
- 方向审核：严格横向侧视、低伏四足、褐红皮毛、前伸吻部与束缚环在 1×均可辨认；视觉宽度约一格半，但逻辑仍为单格。

## 规则冲突与回退

- 总策划案 A.2 写明单位画布按主体向 32 倍数自适应，因此本候选的最小合法画布为 64×32。
- `Tools/OCCArt/occ_art_contract_v1.json` 目前只有新制 `tactical_unit_32x64` 与仅供旧资产的 `tactical_unit_64`，缺少可验证 64×32 的新单位角色。
- 以候选 manifest 调用 `validate_occ_art_asset.py` 时返回空报告并以退出码 1 结束，证明当前机器合同没有可用角色，而不是候选图像本身被现有角色覆盖验证。
- 曾临时替换 Unity 正式资源并验证 64×32 原生比例显示、底部中心锚定和首战绑定均通过；发现合同冲突后已完整恢复原 64×64 正式贴图及原显示代码。当前 Unity 再编译 0 error／0 warning，旧资源 GUID `40aea0710a36fbb4bb1b47c82d8fd3fc` 与 64×64 尺寸均已回读确认。
- 当前状态为 `QA_PENDING`，候选仅保存在归档；等待用户确认是否按总策划案补充 `tactical_unit_adaptive_32ppu` 机器角色后再正式实装。
