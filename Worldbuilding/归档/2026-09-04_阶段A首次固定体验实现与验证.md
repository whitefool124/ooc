# 阶段 A 首次固定体验实现与验证

日期：2026-09-04

## 目标

建立每个新存档一次性的固定首次体验流程骨架，优先消除系统与底层机制差异；不设计尚未冻结的地图、敌人、术式、奖励或 A／B／C 实际效果。

## 实现范围

- 固定首轮程序与版本化快照；旧 `map10` 存档显式标记为 grandfathered，常驻学院数据池不受固定首轮约束。
- 11 个图节点，其中 O 为不计内容的固定出身入口，另有 10 个内容节点；3 场普通战、1 场精英，无 Boss。
- 固定拓扑、分层解锁、无成本跨已完成节点寻路、节点状态、胜败分支、三组固定三选一候选、事件／工坊／医务室／商店一次性状态。
- A／B／C 机制槽、攻击／靠近／进入／经过／互动／受击／破坏触发上下文、敌我同规则约束、移动路径轨迹、悬浮说明、敌人脚本与战斗台词接口。
- 9 个活动装备槽与旧存档槽位迁移。
- 新存档入口改为固定出身三件套；正式战斗内容未接入时明确禁用并显示占位状态。

## 验证证据

- Unity 工程身份：`E:/数据库/OCC_Codex/UnityProject/Assets`。
- 活动场景：`Assets/Scenes/CombatPrototype.unity`，验证前后均未保存或修改场景。
- 脚本重编译：0 error，0 warning。
- 聚焦 EditMode：31/31 通过。
- 全量 EditMode：722/722 通过，0 失败、0 跳过。

## 内容边界

正式地图、敌人、术式、三组奖励、法宝、事件文本、A／B／C 机制与精英奖励形式进入阶段 B。阶段 A 中所有对应 ID 均带 `PHASE_A_PLACEHOLDER` 或 `MECHANIC-SLOT` 标识，不能作为内容定案。
