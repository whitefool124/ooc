# OCC 肉鸽首区完成线回归（2026-08-04）

## 结论

R-F1 至 R-F6 全部通过。首区肉鸽切片具备可持续扩容的系统、正式玩家流程、视觉语义和确定性回归基线；后续主要剩余工作为内容扩容、数值平衡与品质迭代。

## 自动化矩阵

| 验证面 | 结果 |
| --- | --- |
| 固定种子 | `620` / `621`，分别覆盖两种区域首领 |
| 完整路径 | 入口至区域核心，覆盖战斗、公开额外战斗事件、休整、权限、精炼、精英和首领 |
| 存档恢复 | 每个待领奖、待事件战、资源变化、后续战斗和最终完成边界执行 `map5` 文本完全一致往返 |
| 构筑矩阵 | 余烬突击、锚定控制、以太回路、战地工程 × 两名首领，重复运行结果一致 |
| 回访规则 | 已清传输塔与核心前哨可回访，且不会再次启动战斗 |
| 结算规则 | 奖励不可重复点击领取；最终领奖后 `IsComplete=true` |
| 禁止项 | 无倒计时、追击、敌情推进、命中率或暴击率合同 |
| 聚焦测试 | 完成线专项 4/4；相关无参数回归 62 项 |

## 本轮修复

`RogueliteMapRun.FromJson` 原先让访问集与完成集共用一个强制补 `start` 的恢复函数，导致任意版本存档恢复后入口被错误记为已完成。现已区分恢复语义：访问集始终补入口，完成集严格使用存档内容。`map1–map5` 兼容路径保持通过。

## 运行时 UI

- 正式地图生成 20/20 节点。
- 1920×1080 下正式地图按钮均在屏幕边界内。
- 布局使用 1920×1080、`matchWidthOrHeight=0.5` 的 `CanvasScaler`；同一 16:9 参考坐标投影至 960×540 无关键溢出。
- 开发控制台仍为独立、默认隐藏的测试入口。

## 最终环境

- Unity `6000.5.2f1`。
- Funplay 编译错误/警告 0。
- Console 无项目错误。
- 已退出 Play Mode。
- `Assets/Scenes/CombatPrototype.unity` 为 `isDirty=false`；未保存场景。
