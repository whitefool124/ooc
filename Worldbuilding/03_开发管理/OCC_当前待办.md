# OCC 当前待办

> 开发工作的单一事实源。每次开始实现前先读取本文；完成、阻塞或改变范围后立即更新。`当前进行`只能有一个主任务。

## 使用规则

- 每项任务必须写明目标、涉及文件/系统、验收标准与完成后解锁的下一步。
- 玩法改变先更新 `Worldbuilding/01_游戏策划/` 源文件，再同步开发计划与本文。
- 剧情模式不得引入时间压力、敌情推进、倒计时或拖延关闭地点机制。
- Unity 脚本改动必须经 Funplay 重新编译并检查 Console；除非明确要求，不保存场景。

## 当前进行

### V1：战斗表现、结算界面与像素资源完成品基线 - IN PROGRESS

#### R2-00：肉鸽玩法决策问卷 v2 - COMPLETE
- **归属**：肉鸽模式。
- **目标**：通过前端问卷补齐自由回访房间节点图、探索资源、构筑、失败/撤离、事件/工坊、内容密度和禁区的产品决策，避免直接猜测扩展玩法。
- **涉及文件/系统**：`Worldbuilding/03_开发管理/OCC_肉鸽玩法决策问卷_v2.html`、后续肉鸽策划源文件、开发计划与待办。
- **验收标准**：问卷可在浏览器保存草稿并导出 JSON/Markdown；答卷覆盖所有必答项，随后先固化为策划源文件再创建实现任务。
- **验证结果**：已导入 `OCC_肉鸽玩法决策_v2.json`；决策已固化到 `Worldbuilding/01_游戏策划/OCC_肉鸽模式玩法定义_v0.1.md`。
- **完成后解锁**：R2-01 自由回访节点图与房间目录实现。

#### R2-01：自由回访节点图与空间资源门 - COMPLETE（2026-07-23）
- **归属**：肉鸽模式。
- **目标**：把当前线性 5 节点地图替换为约 20 节点的正交自由回访网络，显示完整拓扑与模糊未知类型；加入钥匙/权限卡门，不引入时间、追击或地点关闭。
- **涉及文件/系统**：`RogueliteMapRun`、节点目录、肉鸽独立存档、大地图展示与节点状态。
- **验收标准**：已访问节点可回访、清理战斗房永久安全、钥匙门阻挡未满足条件的连接、剧情存档不受影响，Play Mode 可验证节点往返与状态保存。
- **完成后解锁**：R2-02 节点内容目录与可预览事件。
- **验证结果（2026-07-23）**：完成 20 节点正交房间目录、双向相邻移动、已访问/已清理状态、权限卡门与 `map2` 独立存档；旧 `map1` 存档可迁移。涉及 `RogueliteMapRun`、`RogueliteDeveloperRun`、`CombatPrototypeBootstrap` 与 `RogueliteDeveloperRunTests`。Play Mode 运行时断言验证：入口进入战斗房、返回入口再回访、事件/医疗/许可档案路径获得 1 张权限卡并进入“传输塔”战前简报；另验证战前简报→正式战斗→胜利结算→领奖→返回地图，战斗房保持安全且存档为 `map2`。Funplay 编译错误/警告为 0；Console 仅有既有 `RenderTexture.active` 释放警告。截图工具持续返回陈旧开发菜单帧，1920×1080 / 960×540 仅记录为截图刷新限制，不以其作为 UI 视觉验收证据；场景未保存。
- **下一步**：R2-02 节点内容目录与可预览事件；补全各非战斗房的真实商店、工坊、休整、宝藏及事件结算，保持可回访与无时间压力。

#### R2-02：节点内容目录与可预览事件 - COMPLETE（2026-07-23）
- **归属**：肉鸽模式。
- **目标**：为事件、工坊、商店、休整与宝藏节点建立内容目录；进入非战斗节点先显示明确的风险/收益选择，再结算节点状态，不以自动完成代替玩家决策。
- **涉及文件/系统**：`RogueliteMapRun`、节点内容目录、肉鸽地图 IMGUI 面板、独立存档、EditMode/Play Mode 验证。
- **验收标准**：每类非战斗节点均有可读预览；事件选择明确标注收益与额外战斗后果且不含强制扣血；结算结果随独立存档持久化；回访事件保留收益递减提示；没有倒计时、追击或地点关闭。
- **完成后解锁**：R2-03 双货币、工坊操作、奖励目录与魔法侧构筑数据。
- **验证结果（2026-07-23）**：事件、工坊、商店、休整与宝藏均有两项可读预览选择；非战斗节点不再自动结算。商店“医疗补给”立即结算 +1 补给并保存；事件“超载回收”明确展示“+1 权限卡 / 进入一场额外战斗”，胜利后才结算权限卡，不包含强制扣血。肉鸽独立存档升级为 `map3`，兼容 `map1`/`map2`。Play Mode 运行时断言、内容目录覆盖检查、后续聚焦 EditMode 均通过；无倒计时、追击或地点关闭。Console 无项目错误；退出 Play Mode 时 DOTween Safe Mode 汇总了既有的 missing-target/startup 警告，未提供指向 R2-02 代码的调用栈。
- **下一步**：R2-03 双货币、工坊操作、奖励目录与魔法侧构筑数据。

#### R2-03：双货币、工坊操作与奖励目录 - COMPLETE（2026-07-23）
- **归属**：肉鸽模式。
- **目标**：为肉鸽独立存档加入零件/以太、可预览商店价格、工坊装备替换与可追踪校准；奖励获得后不自动装备。
- **涉及文件/系统**：`OCC_肉鸽模式玩法定义_v0.1.md`、`RogueliteMapRun`、节点内容目录、地图面板、奖励注入、EditMode/Play Mode 验证。
- **验收标准**：战斗产出双货币；商店余额不足会阻止购买且不改变状态；工坊仅能装备已获得奖励；选择的装备与校准实际注入下一场战斗；`map4` 存档兼容旧版且不触碰剧情存档。
- **完成后解锁**：R2-04 单区域敌人、首领与首批流派内容验证。
- **验证结果（2026-07-23）**：肉鸽独立存档升级为 `map4`，兼容 `map1`/`map2`/`map3`。新局默认有 4 零件、2 以太；战斗胜利获得 2 零件、1 以太。商店“医疗补给”消耗 2 零件；余额不足会抛出可读错误且不改变资源。奖励领取后不再自动换装，工坊仅允许装备已拥有的武器/术式；以太校准消耗 2 以太，为下一场战斗实际增加 1 点护甲。Play Mode 实测确认 `arcane_wand` 与校准护甲已注入后续战斗；聚焦 EditMode、Funplay 编译错误/警告 0、Console 无项目错误；未修改场景或剧情存档。
- **下一步**：R2-04 单区域敌人、首领与首批流派内容验证。

#### R2-04：单区域敌人、首领与首批编成验证 - COMPLETE（2026-07-24）
- **归属**：肉鸽模式。
- **目标**：将区域节点绑定确定性敌人编成，交付 8–10 种敌人目录与首个区域首领；精英、首领和普通节点在战前简报及战斗状态中可区分。
- **涉及文件/系统**：`OCC_肉鸽模式玩法定义_v0.1.md`、`EnemyArchetypes`、区域遭遇目录、`CombatPrototypeBootstrap`、EditMode/Play Mode 验证。
- **验收标准**：敌人目录至少 8 种；普通/精英/首领节点采用不同编成；区域核心生成首领的明确高生命/护盾/护甲配置；既有奖励、货币、工坊构筑仍可进入后续战斗；不引入时间压力或场景 YAML 修改。
- **完成后解锁**：R2-05 区域事件扩充、第二首领与流派内容平衡。
- **验证结果（2026-07-24）**：区域敌人目录为 10 种（9 种常规/精英敌人加“核心守备监工”首领）。普通、精英与首领节点均从确定性遭遇目录生成不同编成；首领节点实际生成 30 生命、4 护盾、3 护甲的核心守备监工。Play Mode 按真实正交路径验证信号枢纽普通编成、精英铸造厂、传输塔、核心前哨与区域核心首领；既有奖励结算、权限门和无时间压力规则保持有效。聚焦 EditMode 通过；Funplay 编译错误/警告 0，Console 无项目错误，场景未保存。
- **下一步**：R2-05 区域事件扩充、第二首领与流派内容平衡。

#### R2-05：区域事件扩充、第二首领与流派平衡 - COMPLETE（2026-07-24）
- **归属**：肉鸽模式。
- **目标**：扩充可预览事件结果；为单区域加入第二首领变体；给现有可战斗奖励建立突击/控制/以太流派标记并验证奖励选择跨流派。
- **涉及文件/系统**：`OCC_肉鸽模式玩法定义_v0.1.md`、`RogueliteMapRun`、节点内容目录、区域遭遇目录、地图面板、EditMode/Play Mode 验证。
- **验收标准**：事件三项结果均完整预览且无强制伤血/时间压力；同一局首领由种子稳定决定并随独立存档恢复；两名首领数值/武器不同；每次三选一覆盖至少两条流派。
- **完成后解锁**：R2-06 完整区域回归、数值调优与内容扩容计划。
- **验证结果（2026-07-24）**：事件增加“净化导管”：完整预览 +1 补给、+1 以太、无额外战斗；原有勘测与额外战斗换权限卡路径保留。区域核心根据种子确定“核心守备监工”或“以太净化监工”，身份写入 `map5` 并可恢复；后者为 26 生命、6 护盾、1 护甲的远程以太首领，与重甲近战首领区分。奖励目录标注突击/控制/以太；30 个种子下的三选一均至少覆盖两条流派。Play Mode、聚焦 EditMode、Funplay 编译错误/警告 0、Console 无项目错误；场景未保存。
- **下一步**：R2-06 完整区域回归、数值调优与内容扩容计划。

#### R2-06：完整区域回归、数值基线与扩容计划 - COMPLETE（2026-07-24）
- **归属**：肉鸽模式。
- **目标**：完成一条从入口至区域核心的统一运行时回归，核对事件、商店、工坊、权限门、精英、首领、奖励与独立存档；将可验证数值基线和下一阶段扩容边界写入策划源文件。
- **涉及文件/系统**：`OCC_肉鸽模式玩法定义_v0.1.md`、`RogueliteMapRun`、区域遭遇/节点目录、`CombatPrototypeBootstrap`、EditMode/Play Mode 验证记录。
- **验收标准**：完整路径中无时间压力或强制扣血；首领可达且奖励/货币/存档正确；剧情状态不被访问；编译与 Console 无项目错误；明确记录当前内容上限、数值基线和 R2 后续扩容任务。
- **完成后解锁**：R3-01 事件池扩容与回访递减、R3-02 构筑/装备内容池、R3-03 第二地区与敌人美术资产 QA。
- **验证结果（2026-07-24）**：Play Mode 使用真实正交相邻/回访路径完成入口→铁路巡逻→补给检查站（医疗补给）→野战工坊（奥术魔杖、以太校准）→中继突袭→中继事件（超载额外战斗并获得权限卡）→闸门→精英铸造厂→传输塔→以太精炼厂（净化导管）→回访传输塔→核心前哨→区域核心首领。最终 `IsComplete=True`，`map5` 存档有效，剧情哨兵值未变；本局“核心守备监工”生命 30，最终资源为 16 零件、8 以太、2 补给、1 权限卡、8 项已领奖励。聚焦 EditMode 5/5 通过（首领种子/存档、净化事件、跨流派奖励、区域编成、商店与校准）；Funplay 编译错误/警告 0，Console 无条目，场景未保存且不 dirty。途中一次从净化导管直接跳往核心前哨被正确的相邻规则拒绝，改为先回访传输塔后通过，不构成实现错误。

#### V1 连续交付路线（已锁定）
- **执行顺序**：V1-04 可破坏物与状态效果反馈 → V1-05 战斗 UI 组件与状态规范 → M1-01 大地图节点状态与路径数据 → M1-02 工业城市大地图视觉原型 → V1-06 战斗/大地图/结算统一视觉 QA。
- **统一验收**：完成上述五项后一次性执行 Play Mode、Funplay 编译/Console、聚焦 EditMode、1920×1080 / 960×540 截图与完整流程回归；中途不切换其他主任务。
- **当前主任务**：无；V2-13 已完成。后续美术扩充继续一次只处理一个静态对象或单位静帧，默认不创建多帧动画。

#### V2-03：正式像素资产 QA 基线 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用战斗/界面资产。
- **目标**：盘点并规范正式 `32×32` 图标、`32×32` 地块、`64×64` 单位与后续动画资产，建立“可进入正式资产 / 仅原型 / 仅概念参考”的 QA 门槛；不把 AI 拼板或脚本矩形组件当作正式美术。
- **当前盘点**：`UnityProject/Assets/Game/Art/UI/Icons32/` 现有 `attack`、`interact`、`loot`、`move`、`skillOne`、`skillTwo` 六个 Unity `32×32` 点过滤图标资源；当前没有经过独立帧规范化、基线/中心线报告的 `64×64` 单位动画或正式 `32×32` 地块切片。
- **涉及文件/系统**：`UnityProject/Assets/Game/Art/`、像素资产流水线 QA 输出、Unity 纹理导入设置与后续正式资产清单；不替换未经 QA 的资产，不修改场景 YAML。
- **验收标准**：每个正式资产有固定尺寸、整数缩放、点过滤、透明边界/调色板/基线检查和 QA 记录；图标、地块、单位动画分别验收；未通过资产只能标为原型或概念参考。
- **完成后解锁**：V2-04 首批正式像素资产制作（战斗图标扩充、单位静帧/待机、12×9 地块切片）。
- **验证结果（2026-07-24）**：新增 `OCC_像素资产_QA流程_v0.1.md`，固定 `CONCEPT`/`PROTOTYPE`/`QA_PENDING`/`FORMAL` 状态、原料与 QA 输出目录、通用及分类门禁、审批顺序、独立帧规则和 V2-04 逐项交付清单。通过 Funplay `AssetDatabase` 实测 `Icons32` 六项均为 `32×32`、`RGBA32`、`Point`、`Clamp`、单层纹理；它们缺少独立原料、轮廓语义、透明边界和调色板证据，全部继续标为 `PROTOTYPE`。正式 `64×64` 单位、`32×32` 地块与 12×9 中继站切片仍为 `MISSING`。未导入或替换 Assets、未保存场景；Funplay 编译错误/警告 0，场景 `isDirty=false`。

#### V2-04：首批正式像素资产制作 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用战斗/界面资产。
- **目标**：以独立单图原料制作移动/攻击/技能/搜刮/互动图标，主角/步枪兵/盾卫/火术师/精英静帧，以及中继站地块、轻掩体、重掩体、中继器、战利品箱；不直接导入 Unity 正式 Assets。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-04/`、对应 `QA/` 输出、`OCC_像素资产_QA流程_v0.1.md` 与正式资产清单；后续通过审查后才涉及 Unity 纹理导入设置。
- **验收标准**：图标/对象为 `32×32`，单位为 `64×64` 且中心线 `X=32`、脚底基线 `Y=58`；每项有独立原料、4x 审查预览、调色板与 QA 报告；无 AI 拼板硬切、伪雪碧图或正式 Assets 导入；动画只在静帧批准后以独立帧开展。
- **完成后解锁**：V2-05 正式导入与首批运行时替换评审（仅限通过 `FORMAL` QA 的资产）。
- **阶段验证（2026-07-24 / 主角原料）**：将既有独立主角绿幕原料登记至 `Worldbuilding/05_美术与音频/像素资产原料/V2-04/Units64/`，并用像素资产流水线生成 `64×64` 单帧、4x 中心线/基线 QA 图、报告与预览。自动报告通过固定 cell、硬 alpha、24 色限制，规范化边界为 `[22,12,42,58]`，符合 `X=32` / `Y=58` 对齐；人工审查判定目标尺寸下步枪与身体细节混杂，且未做 Aseprite/PixelOver 手工清理，继续标记 `QA_PENDING`，不导入 Unity。工作台的 15 项独立原料请求和一次节流重试均收到上游 `429 Too Many Requests`；未用 AI 拼板硬切或脚本图形替代，待额度恢复后继续生成余下原料。未修改场景 YAML 或 `UnityProject/Assets/`。
- **阻塞原因（2026-07-24）**：本地工作台可启动且本机凭据/中转配置存在，但后续两轮单图复测及此前的批量请求均被上游稳定拒绝为 `429 Too Many Requests`。内建图像生成工具在当前会话不可用；本机未发现可合法纳入 OCC 的其余独立原料。不得以其他项目资源、概念拼板、AI 硬切或脚本矩形替代。
- **恢复条件 / 所需决定**：恢复上游图像生成额度后，继续按既定 15 项独立单图提示词生成；或由产品提供已获授权的独立原料与使用范围。恢复后先将本任务重新设为唯一当前主任务，再继续 QA，不直接导入 Unity。
- **验证结果（2026-07-24 / 原料覆盖）**：更新本机工作台令牌后，上游调用恢复。完成 5 个指令图标、5 个单位静帧和 5 个中继站对象的独立单图原料；每项已归档到 `像素资产原料/V2-04/`，并完成固定 cell、硬 alpha、4x QA、调色板预览和 JSON 报告。15 份报告经 Node JSON 解析均有效；图标/对象为 `32×32`，单位为 `64×64` 且 QA 标记 `X=32` / `Y=58`。所有项目保持 `QA_PENDING`，未导入 `UnityProject/Assets/`；集中审查已明确移动/攻击图标、地块色数和单位细节的手工清理要求。

#### V2-04b：首批像素资产规范化与正式审批 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用战斗/界面资产。
- **目标**：以本地独立生图原料加像素规范化处理完成逐项审批，确认图标轮廓、单位主特征和地块/掩体表面语义；只将逐项通过规格 QA 的资产升级为原料库 `FORMAL`。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-04/`、QA 输出与 `OCC_正式像素资产清单_v0.1.md`；不修改场景 YAML，不直接替换 Unity 现有原型资源。
- **验收标准**：32px 图标/对象为 16 色或更少且表面含义可读，64px 单位对齐 `X=32`/`Y=58` 并有可辨主特征；全部复核 4x、透明边界、调色板和报告。手工清理与高保真美术不作为本批阻塞项。
- **完成后解锁**：V2-05 正式导入与首批运行时替换评审（仅限 `FORMAL` 资产）。
- **验证结果（2026-07-24 / 规范化批准）**：按产品确认的“规格符合、表面含义可读”标准，复核本地独立生图与规范化输出。14 项目录化报告均为 `PASS`，并同时存在 `qa_4x.png`、`palette_4x.png` 与 JSON 报告；主角的独立归档报告也为 `PASS`，`64×64` 边界 `[22,12,42,58]` 符合 `X=32` / `Y=58`。5 图标和 5 中继站对象均为 `32×32` / 16 色；5 单位均为 `64×64` / 24 色。集中审查已批准 15 项为原料库 `FORMAL`，未导入 `UnityProject/Assets/`、未替换运行时原型、未保存场景。Aseprite 试验画布未保存且未纳入交付。

#### V2-05：正式导入与首批运行时替换评审 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用战斗/界面资产。
- **目标**：只将通过 V2-04b 规范化审批的原料分批导入 Unity，并在不保存场景的前提下验证运行时读取、Point/Clamp/无 mipmap、整数尺寸和视觉层级。
- **涉及文件/系统**：`UnityProject/Assets/Game/Resources/Art/FormalIcons32/`、`FormalCombatHud`、后续中继站/单位运行时表现；不修改场景 YAML，不覆盖 `Icons32/*.asset` 原型资源。
- **验收标准**：每批导入都有源原料映射、`Sprite` / Point / Clamp / 无 mipmap 检查和 Play Mode 实例证据；运行时只使用通过审批的 PNG，入口/地图/结算流程不回归。
- **已完成子目标（2026-07-24 / 指令图标）**：从 V2-04 QA 的独立 `frame_00.png` 导入 `move`、`attack`、`skill`、`loot`、`interact` 至 `Resources/Art/FormalIcons32/`，保留旧 `Art/UI/Icons32/*.asset` 原型不变。`FormalCombatHud` 为六个指令按钮建立正式图标层（两格技能共用 `skill`），Play Mode 验证 6 个实例均为 `32×32`、`Point`；编辑器复核为 Sprite、Clamp、无 mipmap。Funplay 编译错误/警告 0；场景未保存。
- **已完成子目标（2026-07-24 / 中继站视觉）**：导入 `floor`、`light_cover`、`heavy_cover`、`relay`、`loot_crate` 至 `Resources/Art/FormalRelay32/`，全部复核为 `32×32`、Sprite、Point、Clamp、无 mipmap。运行时只替换既有 `地图可视化` SpriteRenderer：Play Mode 验证 108 地块、2 轻掩体、2 重掩体和 1 中继器均使用正式图，回退占位为 0。`loot_crate` 已合规导入，当前地图无真实战利品箱节点，故未凭空创建节点或保存场景。
- **已完成子目标（2026-07-24 / 战利品箱绘制接入）**：在 `CombatPrototypeBootstrap` 的既有战利品容器 IMGUI 绘制中优先使用 `FormalRelay32/loot_crate`，贴图缺失时保留文字占位回退；未新增玩法、节点或场景写入。退出 Play Mode 后重编译通过，Play Mode 复核 `loot_crate` 为 `32×32`、Point、Clamp，Console 无新增错误，场景 `isDirty=false`。
- **已完成子目标（2026-07-24 / 单位静帧）**：导入 `hero`、`rifleman`、`shieldguard`、`pyromancer`、`elite` 至 `Resources/Art/FormalUnits64/`，均为 `64×64`、Sprite、Point、Clamp、无 mipmap。战斗网格实际绘制正式贴图，保留生命条、意图与选中轮廓；Play Mode 验证 5/5 贴图可加载，默认战斗的主角、步枪兵、盾卫、火术师与狙击手使用正式映射，精英先锋及两类监工映射至 `elite`。没有对应原料的突袭者保持原有标记，未伪造兵种外观或保存场景。
- **全批回归（2026-07-24）**：Play Mode 进入正式战斗后，统一验证 15/15 已批准资源均可运行时加载，尺寸、Point 和 Clamp 均符合规格；正式 HUD 有 6 个指令图标实例（两格技能共用图标），地图有 108 地块、2 轻掩体、2 重掩体、1 中继器正式实例，默认编成有 5 个正式单位映射。Funplay 编译错误/警告 0，Console 无项目错误，退出 Play Mode 后场景 `isDirty=false`，未保存场景。
- **完成后解锁**：后续美术扩充先按单一主任务处理：战利品箱在实际节点出现后绑定、突袭者独立静帧，或在批准静帧基础上制作独立帧动画；不得以现有静帧伪造动画。

#### V2-06：突袭者独立静帧补齐 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗资产。
- **目标**：为现有“突袭者”生成一张独立原料并规范化为正式 `64×64` 静帧，中心线 `X=32`、脚底基线 `Y=58`；通过 QA 后接入既有运行时单位映射。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-06/`、像素资产 QA 输出、`Resources/Art/FormalUnits64/` 与 `CombatPrototypeBootstrap`；不修改场景 YAML，不伪造动画。
- **验收标准**：原料为单独生成的一张图而非拼板切图；输出具备固定 cell、硬透明边界、受控调色板、4x 审查图和 JSON 报告；Unity 导入为 Sprite / Point / Clamp / 无 mipmap，Play Mode 中突袭者实际使用新贴图。
- **完成后解锁**：下一个独立美术任务可在“批准静帧基础上的独立帧动画”或“新兵种独立静帧”中择一建立，仍保持单主任务。
- **验证结果（2026-07-25）**：本地工作台生成一张突袭者独立原料，`report.json` 为 PASS，规范化边界 `[16,12,49,58]`，符合 `64×64`、`X=32`、`Y=58`、硬 alpha 与 24 色限制；4x QA、调色板预览与审查记录齐全并批准为 `FORMAL`。导入 `Resources/Art/FormalUnits64/raider.png` 后，Funplay 重编译错误/警告 0；Play Mode 复核贴图为 `64×64`、Point、Clamp，并通过运行时反射确认 `突袭者` 实际映射 `raider`。退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-07：主角四帧待机动画 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗资产。
- **目标**：基于已批准主角静帧制作四张独立的待机呼吸循环原料，并统一规范化为 `64×64`、中心线 `X=32`、脚底基线 `Y=58` 的循环；通过 QA 后接入战斗网格的主角绘制。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-07/`、`Resources/Art/FormalAnimations64/`、`CombatPrototypeBootstrap`；不从拼板切帧、不改场景 YAML、不改变战斗规则。
- **验收标准**：四帧均为逐张生成的独立文件，使用同一主角参考而非伪雪碧图；产物含独立帧、`256×64` strip、GIF、4x QA、调色板与 JSON 报告；导入纹理为 Sprite / Point / Clamp / 无 mipmap，Play Mode 中主角绘制按固定循环帧变化。
- **完成后解锁**：下一项仅可在另一兵种独立待机动画或单一动作动画中选择其一，仍须逐帧独立生成与 QA。
- **验证结果（2026-07-25）**：以已批准主角静帧为共同参考，通过本地工作台逐张生成 4 个待机呼吸/重心变化帧；每帧独立归档，未从拼板硬切。规范化报告为 PASS：4 帧均为 `64×64`、每帧 24 色、硬 alpha、`Y=58` 脚底基线与 `X=32` 中心线；已产出 `256×64` strip、GIF、4x QA 与调色板预览，并批准原料库 `FORMAL`。导入 `Resources/Art/FormalAnimations64/hero_idle_4f.png` 后，Funplay 编译错误/警告 0；Play Mode 复核纹理为 `256×64`、Point、Clamp，运行时字段已绑定且循环计算有效（`frame=1/4`）。退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-08：单位静帧低成本运动表现 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗资产。
- **目标**：停止使用未成熟的本地多帧生成作为运行时依赖，保留已批准单位静帧，并以全单位的整像素低幅度垂直微位移表达待机状态。
- **涉及文件/系统**：`CombatPrototypeBootstrap` 与现有 `FormalUnits64` 静帧；撤回本轮未批准、未导入的突袭者多帧原料，不修改场景 YAML，不改变战斗规则。
- **验收标准**：所有正式单位仍按现有静帧映射；待机位移保持整数像素、幅度不超过 1px、不产生模糊或时间压力；Funplay 编译/Console 通过且场景不保存。
- **完成后解锁**：下一项美术扩充优先选择独立静帧或单一反馈表现；只有本地多帧一致性成熟并由产品复核后才重新创建动画任务。
- **验证结果（2026-07-25）**：按产品反馈停止 V2-08 的未提交突袭者四帧原料、QA 与 Unity 导入，不将其列为正式资产。`CombatPrototypeBootstrap` 改为所有正式单位使用既有 `FormalUnits64` 静帧，加相位错开的整像素垂直微位移；运行时复核主角静帧为 `64×64`、Point、Clamp，位移为整数且幅度 `≤1px`，`raider_idle_4f` 资源不存在。Funplay 编译错误/警告 0，Console 无项目错误，退出 Play Mode 后场景 `isDirty=false`，未保存场景。

#### V2-09：中继站静态地块变体 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗地图资产。
- **目标**：补充普通工业地面、轨道地面、警戒地面三张独立静态 `32×32` 原料；通过规格 QA 后替换现有 12×9 中继站地图中的既有 `SpriteRenderer`，提升路线与危险区的表面可读性。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-09/`、`Resources/Art/FormalRelay32/`、`CombatPrototypeBootstrap`；不从拼板切图、不保存场景、不改变 tile 碰撞或战斗规则。
- **验收标准**：三张输入均独立生成；输出为 `32×32`、16 色或更少、硬 alpha/不透明地面边界、4x QA、调色板和 JSON 报告；Unity 导入为 Sprite / Point / Clamp / 无 mipmap，Play Mode 中三类地块都有正式实例。
- **完成后解锁**：下一项只可扩展另一个单一静态环境对象或单位静帧；继续默认静帧与低成本反馈，不创建多帧动画。
- **验证结果（2026-07-25）**：本地工作台独立生成普通工业、轨道、警戒三张地块原料；规范化报告均 PASS，均为 `32×32`、16 色、硬 alpha，并具备 4x QA、调色板与 JSON 报告。导入 `FormalRelay32` 后 Funplay 重编译错误/警告 0；Play Mode 复核 108 格中普通 78、轨道 24、警戒 6，三类贴图均 `32×32`、Point，回退 0。退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-10：中继器破损静态反馈 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗地图资产。
- **目标**：为现有可破坏中继器目标补充一张独立 `32×32` 破损/熄灭状态贴图，在 `TileState.IsDestroyed` 为真时替换既有目标 Sprite；不新增玩法状态，只提升完成反馈可读性。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-10/`、`Resources/Art/FormalRelay32/relay_destroyed.png`、`CombatPrototypeBootstrap`；不改目标规则、不保存场景。
- **验收标准**：独立原料规范化为 `32×32`、16 色或更少、硬 alpha、4x QA、调色板和 JSON 报告；Unity 导入 Sprite / Point / Clamp / 无 mipmap；目标未摧毁时使用正常 `relay`，摧毁后使用 `relay_destroyed`。
- **完成后解锁**：后续美术扩充仍保持单一静态对象或单位静帧任务，默认不创建多帧动画。
- **验证结果（2026-07-25）**：本地工作台独立生成并规范化 `relay_destroyed`，报告 PASS，`32×32`、16 色、硬 alpha、4x QA 与调色板齐全。Unity 导入复核为 Sprite / Point / Clamp / 无 mipmap；Play Mode 确认目标初始 `destroyed=false` 使用完整 `relay`，破损资源可加载且目标绘制逻辑按 `TileState.IsDestroyed` 切换。Funplay 编译错误/警告 0，退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-11：轻掩体破损静态反馈 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗地图资产。
- **目标**：为轻掩体补充一张独立 `32×32` 破损贴图，在既有 `TileState.IsDestroyed` 为真时替换轻掩体视觉；不新增状态或玩法。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-11/`、`Resources/Art/FormalRelay32/light_cover_destroyed.png`、`CombatPrototypeBootstrap`；不改掩体耐久规则、不保存场景。
- **验收标准**：独立原料规范化为 `32×32`、16 色或更少、硬 alpha、4x QA、调色板和 JSON 报告；Unity 导入 Sprite / Point / Clamp / 无 mipmap；轻掩体耐久归零后显示破损图，未破坏时仍显示正常图。
- **完成后解锁**：后续美术扩充继续一次只处理一个静态对象或单位静帧，默认不创建多帧动画。
- **验证结果（2026-07-25）**：本地工作台独立生成并规范化 `light_cover_destroyed`，报告 PASS，`32×32`、16 色、硬 alpha、4x QA 与调色板齐全。Unity 导入复核为 Sprite / Point / Clamp / 无 mipmap；Play Mode 确认轻掩体初始 `durability=4`、`destroyed=false` 使用正常 `light_cover`，破损资源可加载且绘制逻辑按 `TileState.IsDestroyed` 切换。Funplay 编译错误/警告 0，退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-12：重掩体破损静态反馈 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗地图资产。
- **目标**：为重掩体补充一张独立 `32×32` 破损贴图，在既有 `TileState.IsDestroyed` 为真时替换重掩体视觉；不新增状态或玩法。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-12/`、`Resources/Art/FormalRelay32/heavy_cover_destroyed.png`、`CombatPrototypeBootstrap`；不改掩体耐久规则、不保存场景。
- **验收标准**：独立原料规范化为 `32×32`、16 色或更少、硬 alpha、4x QA、调色板和 JSON 报告；Unity 导入 Sprite / Point / Clamp / 无 mipmap；重掩体耐久归零后显示破损图，未破坏时仍显示正常图。
- **完成后解锁**：后续美术扩充继续一次只处理一个静态对象或单位静帧，默认不创建多帧动画。
- **验证结果（2026-07-25）**：本地工作台独立生成并规范化 `heavy_cover_destroyed`，报告 PASS，`32×32`、16 色、硬 alpha、4x QA 与调色板齐全。Unity 导入复核为 Sprite / Point / Clamp / 无 mipmap；Play Mode 确认重掩体初始 `durability=7`、`destroyed=false` 使用正常 `heavy_cover`，破损资源可加载且绘制逻辑按 `TileState.IsDestroyed` 切换。Funplay 编译错误/警告 0，退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-13：战利品箱开启静态反馈 - COMPLETE（2026-07-25）
- **归属**：剧情模式与肉鸽模式共用战斗地图资产。
- **目标**：为战利品箱补充一张独立 `32×32` 开启/空箱贴图，在 `LootContainer.IsLooted` 为真时替换现有箱体视觉；不改变搜刮奖励、货币或交互规则。
- **涉及文件/系统**：`Worldbuilding/05_美术与音频/像素资产原料/V2-13/`、`Resources/Art/FormalRelay32/loot_crate_open.png`、`CombatPrototypeBootstrap`；不保存场景。
- **验收标准**：独立原料规范化为 `32×32`、16 色或更少、硬 alpha、4x QA、调色板和 JSON 报告；Unity 导入 Sprite / Point / Clamp / 无 mipmap；未搜刮时显示正常箱，已搜刮后显示开启箱。
- **完成后解锁**：后续美术扩充继续一次只处理一个静态对象或单位静帧，默认不创建多帧动画。
- **验证结果（2026-07-25）**：本地工作台独立生成并规范化 `loot_crate_open`，报告 PASS，`32×32`、16 色、硬 alpha、4x QA 与调色板齐全。Unity 导入复核为 Sprite / Point / Clamp / 无 mipmap；Play Mode 确认战利品箱初始 `looted=false` 使用关闭箱，开启箱资源可加载，绘制逻辑按 `LootContainer.IsLooted` 选择对应贴图。Funplay 编译错误/警告 0，退出 Play Mode 后 Console 无项目错误，场景 `isDirty=false`，未保存场景。

#### V2-01：正式战斗 HUD 与独立开发控制台 - IN PROGRESS（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用界面表现。
- **目标**：以 1920x1080 的 75% 战场 / 25% HUD 为基准，建立运行时正式战斗 HUD（指令、角色资源、技能、行动条、快捷栏、事件记录），并把开发测试操作迁移至可单独呼出的控制台，不再作为主流程的常驻视觉内容。
- **涉及文件/系统**：`CombatPrototypeBootstrap`、新的运行时战斗 UI 表现层、`TacticalHudSceneBinder`、DOTween；不修改场景 YAML，不导入未经 QA 的 AI 图，不改变任何战斗或肉鸽规则。
- **验收标准**：正式战斗时主 HUD 可读、按钮可操作且与当前指令同步；F1/界面入口可开关开发控制台；控制台关闭时不遮挡战场和 HUD；入口、简报、地图、结算与返回流程保持可回归；1920x1080 / 960x540 无关键内容溢出。
- **完成后解锁**：V2-02 全流程页面逐页视觉重构（入口、地图、选择、工坊、简报、结算）与 V2-03 像素资产 QA。
- **阶段验证（2026-07-24）**：新增 `FormalCombatHud` 与 `DeveloperConsolePanel`，正式战斗运行时断言确认 HUD、独立控制台、指令选中态均存在并可切换；旧场景 HUD 在运行时停用，避免空绑定器遮挡正式界面。Funplay 重编译通过，编译错误/警告 0；Play Mode Console 无新增错误；场景 `isDirty=false`。截图工具仍缓存入口帧，正式 HUD 双分辨率视觉截图待后续刷新链修复后补验。发现并修复旧 `UnityEngine.Input` 与 Input System 冲突。
- **阶段验证（2026-07-24 / 目标读数）**：正式 HUD 增加目标锁定读数，显示目标名称、生命与护盾；运行时断言确认敌方目标绑定、攻击指令选中态和 F1 控制台切换均通过。Funplay 重编译 0 错误/警告，Console 无新增错误；未保存场景。
- **阶段验证（2026-07-24 / 快捷栏与结算）**：正式 HUD 增加技能/快捷栏槽位和胜负覆盖层；快捷栏读取真实 `CombatState.Quickbar`，结果覆盖层提供战术重开与返回入口。Play Mode 运行时断言确认快捷栏首槽为“医疗包”、胜利阶段显示结果状态，控制台保持可切换；Funplay 编译 0 错误/警告，Console 无新增错误，场景 `isDirty=false`。

#### V2-02：全流程页面视觉重构 - COMPLETE（2026-07-24）
- **归属**：肉鸽模式地图与共用流程界面表现。
- **目标**：将入口、肉鸽地图、节点选择、工坊、战前简报、结算页面逐页统一为正式工业控制台视觉；本阶段先完成大地图资源条与当前节点读数。
- **涉及文件/系统**：`CombatPrototypeBootstrap` 地图运行时 IMGUI、`RogueliteMapRun` 状态数据、DOTween；不改变自由回访、权限门或资源规则，不保存场景。
- **阶段验证（2026-07-24 / 地图）**：地图页新增当前节点、种子/等级、零件/以太/补给/权限卡资源芯片；Play Mode 运行时断言确认新局 `start`、3 个可进入节点、4 零件与 2 以太均保持有效。Funplay 编译 0 错误/警告，Console 无新增错误，场景 `isDirty=false`。
- **阶段验证（2026-07-24 / 选择与工坊）**：统一非战斗节点选择面板与野战工坊面板的宽度、留白、状态文案和资源代价；三项选择按可用宽度自适应，避免 1920x1080 右侧溢出。Play Mode 断言确认地图节点选择仍能进入真实节点状态；Funplay 编译 0 错误/警告，Console 无新增错误，场景 `isDirty=false`。
- **阶段验证（2026-07-24 / 入口与简报）**：战前简报改为正式工业控制台布局，增加剧情/肉鸽上下文、节点名称、任务目标、敌方编成、无倒计时/重开规则、肉鸽资源芯片及确认/返回动作。Play Mode 断言确认剧情简报与肉鸽节点简报上下文均有效；Funplay 编译 0 错误/警告，Console 无新增错误，场景 `isDirty=false`。
- **阶段验证（2026-07-24 / 入口与结算）**：入口文案改为“正式行动入口”，明确剧情/肉鸽两条玩家流程，开发控制台改为战斗中 F1 呼出；肉鸽结算文案明确奖励三选一、保存推进状态与“不自动装备”。Play Mode 断言通过剧情入口→战斗结果、肉鸽节点→战斗胜利→三项奖励结算；Funplay 编译 0 错误/警告，Console 无新增错误，场景 `isDirty=false`。
- **阶段验证（2026-07-24 / 全流程回归）**：Play Mode 完成剧情入口→战前简报→正式战斗→战术重开→返回入口，以及肉鸽地图→节点战斗→胜利结算→领取奖励→返回地图；奖励领取后可进入节点为 4。1920x1080 与 960x540 截图文件均成功生成，但阶段切换截图仍可能缓存入口帧，视觉阶段证据以运行时状态断言为主。Funplay 编译 0 错误/警告；Console 仅 2 条既有 `RenderTexture.active` 释放警告；场景 `isDirty=false`。
- **下一步**：V2-02 已完成；进入 V2-03 像素资产 QA 前，先确认正式资产清单与优先级。

#### V1-06：战斗/大地图/结算统一视觉 QA - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用战斗表现。
- **目标**：对 V1-04、V1-05、M1-01、M1-02 与既有结算界面执行同一轮运行时流程、双分辨率、编译、Console 和聚焦 EditMode 验收。
- **涉及文件/系统**：`CombatVisualFeedback`、`RogueliteSettlementPresentation`、`TacticalHudSceneBinder`、`CombatPrototypeBootstrap`、`RogueliteMapRun`；本次仅更新本待办验证记录，不修改场景 YAML 或玩法规则。
- **验收标准**：开发菜单、简报、战斗、攻击/状态/可破坏物反馈、战术重开、返回入口、地图节点、胜利结算与奖励领取可回归；1920x1080 和 960x540 不溢出；编译/Console 无项目错误；场景不 dirty。
- **验证结果（2026-07-24）**：Play Mode 运行时断言通过剧情入口→战前简报→正式战斗→攻击/状态/可破坏物 DOTween 反馈→战术重开→返回入口；肉鸽地图新局→铁路巡逻→正式战斗→胜利→三选一奖励领取→返回地图也通过，奖励领取后可进入节点为 4。`Camera.main`、`CombatVisualFeedback` 与 `RogueliteSettlementPresentation` 均存在；反馈层运行时生成攻击、燃烧与可破坏物提示对象。聚焦 EditMode 5/5 通过：`FireBolt_AppliesBurningAndCooldown_Deterministically`、`BoundUnit_CannotMove`、地图视觉状态、商店/工坊持久化、跨流派奖励。Funplay 编译错误/警告 0；Console 仅 2 条既有 `RenderTexture.active` 释放警告，无项目错误；`Assets/Scenes/CombatPrototype.unity` 为 `isDirty=false`。1920x1080 与 960x540 截图实际可见、无溢出且菜单文字可读；截图工具在阶段切换后仍缓存入口帧，因此地图/战斗阶段以真实运行时状态断言作为验收依据。IMGUI 不提供 uGUI 射线命中，无法用 `simulate_mouse_click` 单独证明控件命中，但流程公开方法与状态完成验证均通过。
- **完成后解锁**：R3-01 事件池扩容与回访递减、R3-02 构筑/装备内容池、R3-03 第二地区与敌人美术资产 QA；需由产品确认优先级后再开始。
- **归属**：剧情模式与肉鸽模式共用战斗表现。
- **V1-04 目标**：为中继器、掩体等可破坏物提供受损/摧毁反馈，并建立燃烧、束缚、破甲、护盾等状态的统一视觉语义。
- **涉及文件/系统**：`CombatVisualFeedback`、`CombatPrototypeBootstrap`、可破坏物运行时表现、状态效果表现；不直接修改场景 YAML。
- **验收标准**：受损与摧毁状态可读，状态颜色与 HUD 语义一致，表现由 DOTween 驱动且不遮挡 HUD；完成后解锁 V1-05。
- **验证结果（2026-07-24）**：`CombatVisualFeedback` 现为掩体/中继器按耐久变化显示受损或摧毁脉冲、浮字与击破提示；燃烧（锈红）、束缚（冷青）、破甲（安全黄）和缓慢（灰绿）在施加时显示 DOTween 脉冲/标签，并在单位格底边保留不遮挡主体的颜色标记。Play Mode 运行时验证轻/重掩体与目标中继器、四种状态均生成可读反馈（5 项标签）；聚焦 EditMode 3/3 通过；Funplay 编译错误/警告 0，Console 无新增错误，仅两项既有 `FindFirstObjectByType` 弃用警告；场景 `isDirty=false`。1920×1080 / 960×540 截图文件可生成但仍返回陈旧开发菜单帧，记录为截图刷新限制，不作为视觉验收依据。
- **下一步**：V1-05 战斗 UI 组件与状态规范。

#### V1-05：战斗 UI 组件与状态规范 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用战斗表现。
- **目标**：让场景化战术 HUD 明确呈现当前指令、行动单位、资源变化、单位状态和行动条优先级，并将既定冷青/锈红/安全黄/灰绿语义同步到 HUD，保持 75% 战场 / 25% HUD 的现有结构。
- **涉及文件/系统**：`TacticalHudSceneBinder`、`CombatPrototypeBootstrap`、现有场景 HUD 绑定与 DOTween；不保存场景或修改场景 YAML。
- **验收标准**：选择指令有持续可读的选中状态；状态名称使用中文且颜色一致；生命/护盾/以太变动有克制的 DOTween 反馈；行动条标识当前行动单位；运行时不遮挡地图与 HUD，不引入新规则。完成后解锁 M1-01。
- **验证结果（2026-07-24）**：场景化 HUD 现将攻击等当前指令保持为冷青选中态；行动条使用 `▶` 与冷青标识当前行动单位；主手状态显示燃烧（锈红）、束缚（冷青）、破甲（安全黄）、缓慢（灰绿）的真实中文标签。生命/护盾/以太条变化使用 DOTween 填充过渡与轻微缩放反馈。Play Mode 运行时断言确认指令、四色状态、行动条和生命条 Tween 均生效；聚焦 EditMode 3/3 通过；Funplay 编译错误/警告 0，Console 无新增错误，仅既有 `FindFirstObjectByType` 弃用警告；场景未保存且 `isDirty=false`。1920×1080 截图仍刷新为陈旧开发菜单帧，记录为验证限制。
- **下一步**：M1-01 大地图节点状态与路径数据。

#### V1-Menu：开发菜单视觉更新 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用入口。
- **目标**：把开发测试菜单更新为与战术 HUD 一致的极简工业控制台，清楚区分剧情行动与肉鸽区域入口，不改变现有流程或规则。
- **涉及文件/系统**：`CombatPrototypeBootstrap` 运行时 IMGUI、DOTween；不修改场景 YAML。
- **验收标准**：入口文案、任务摘要和路径说明清晰；剧情/肉鸽按钮语义色可区分且能实际进入原流程；1920×1080 与 960×540 不溢出；完成后回到 M1-01。
- **验证结果（2026-07-24）**：运行时菜单更新为深色极简工业控制台，使用冷青“剧情行动”和安全黄“肉鸽区域”两张信息卡、对应大入口与完整流程提示；以 DOTween 完成克制的入场透明度/缩放。Funplay Game View 在 1920×1080 和 960×540 均可见、无文字重叠或溢出；运行时验证剧情简报与肉鸽新局入口均可用（新局保持 4 零件/2 以太）。IMGUI 不暴露 uGUI 射线命中，`simulate_mouse_click` 未命中属于既有工具限制；编译错误/警告 0，Console 无新增错误，场景未保存。
- **下一步**：M1-01 大地图节点状态与路径数据。

#### V1-FullFlow：全流程界面统一更新 - COMPLETE（2026-07-24）
- **归属**：剧情模式与肉鸽模式共用流程表现。
- **目标**：将开发入口、肉鸽配置、推进地图、节点/工坊/奖励选择、短局事件、战前简报与战斗流程栏统一为极简工业控制台视觉，避免只有首层菜单更新。
- **涉及文件/系统**：`CombatPrototypeBootstrap` 的运行时 IMGUI 与 DOTween；不修改场景 YAML 或任何玩法规则。
- **验收标准**：各阶段均使用一致面板/按钮/状态色；剧情、肉鸽和返回路径可回归；1920×1080 / 960×540 无溢出；完成后恢复 M1-01。
- **验证结果（2026-07-24）**：统一的运行时控制台面板/按钮已覆盖肉鸽配置、区域地图、节点状态、事件选择、工坊装备/校准、三选一奖励、短局事件、战前简报与战斗流程栏；冷青用于剧情/可执行操作，安全黄用于肉鸽推进/奖励，锈红仅用于危险或删除。Play Mode 回归通过地图→内容选择→战斗奖励→工坊→简报→正式战斗→战术重开；聚焦 EditMode 4/4 通过。Funplay 编译错误/警告 0，Console 无新增错误，场景未保存且 `isDirty=false`。截图工具在阶段切换后仍返回缓存菜单画面，记录为验证限制，视觉布局以已验证的菜单双分辨率截图与运行时流程状态为证据。
- **下一步**：M1-01 大地图节点状态与路径数据。

#### M1-01：大地图节点状态与路径数据 - COMPLETE（2026-07-24）
- **归属**：肉鸽模式大地图表现。
- **目标**：将自由回访网络的当前、可进入、权限门、已清理、已访问、已知未探索与未知状态收敛为可测试的数据接口，并驱动节点及路径的统一视觉状态。
- **涉及文件/系统**：`RogueliteMapRun`、`RogueliteDeveloperRunTests`、`CombatPrototypeBootstrap` 地图运行时 IMGUI；不修改场景 YAML。
- **验收标准**：状态与既有相邻/权限卡规则一致，已清理房间可读且安全，未知房间保持模糊，路径显式区分可走/已探索/权限受限；状态随独立存档恢复。完成后解锁 M1-02。
- **验证结果（2026-07-24）**：新增 `RogueliteMapNodeVisualState` 与 `VisualStateFor`/`AvailableNodes` 数据接口，统一输出当前位置、可进入、权限门、已清理、已访问、已知未探索与未知；地图节点和路径只消费该状态。可走路径为冷青、已探索路径为灰绿、权限受限为锈红、未知为低饱和灰；节点中文类型/状态与既有相邻、回访、权限卡规则一致。Play Mode 验证入口/可进入/未知核心/权限受限传输塔及可进入列表；聚焦 EditMode 3/3 通过；Funplay 编译错误/警告 0、Console 为空、场景未保存且 `isDirty=false`。
- **下一步**：M1-02 工业城市大地图视觉原型。

#### M1-02：工业城市大地图视觉原型 - COMPLETE（2026-07-24）
- **归属**：肉鸽模式大地图表现。
- **目标**：将 20 节点正交网络呈现为“铁路前线—工业网格—核心隔离区”的近代魔导工业区域图，加入可读的铁路、导管、区域层级和状态图例。
- **涉及文件/系统**：`CombatPrototypeBootstrap` 地图运行时 IMGUI、M1-01 节点状态数据、DOTween；不使用 AI 图或修改场景 YAML。
- **验收标准**：区域层级不覆盖节点、路径或选择面板；可走/已探索/权限门/未知图例与实际状态一致；1920×1080 / 960×540 可读；剧情和肉鸽规则不变。完成后解锁 V1-06。
- **验证结果（2026-07-24）**：地图运行时增加“铁路前线—工业网格—核心隔离区”三层区域底图、铁路轨枕、以太导管与四项状态图例；底图在路径和节点下方绘制，不改变 M1-01 数据或操作逻辑。Play Mode 确认新局地图入口可用、3 个初始可进入节点保持有效；聚焦 EditMode 3/3 通过；Funplay 编译错误/警告 0、Console 无新增错误、场景 `isDirty=false`。1920×1080 / 960×540 截图文件可生成但仍缓存开发菜单帧，记录为截图刷新限制。
- **下一步**：V1-06 战斗/大地图/结算统一视觉 QA。

#### V1-03：攻击源/命中目标/击破地图内视觉反馈 - COMPLETE（2026-07-23）
- **目标**：为攻击、技能、命中、击破和目标选择提供不遮挡 HUD 的运行时视觉反馈。
- **涉及文件**：`UnityProject/Assets/Game/Runtime/Presentation/CombatVisualFeedback.cs`、`UnityProject/Assets/Game/Runtime/Presentation/CombatPrototypeBootstrap.cs`。
- **验收结果**：攻击源与目标格 DOTween 脉冲、选中目标黄色轮廓、伤害数字锚定地图格、击破提示均已接入；击破单位因 `IsAlive` 过滤不再绘制行动条/意图/交互状态；未修改场景 YAML。
- **验证**：Funplay 重编译完成；编译错误/警告 0。Play Mode 已进入战斗并确认 `IsDeveloperCombatActive=True`；运行时调用 `NotifyAttack` 成功生成攻击/命中反馈对象（4 个）及击破反馈对象（8 个）；1920×1080 与 960×540 截图均成功生成。Console 无新增运行时错误，仅既有 `FindFirstObjectByType` 弃用警告。
- **下一步**：继续扩展同一表现层的可破坏物受损/摧毁反馈；不引入新战斗规则。

- **归属**：肉鸽模式与共用战斗表现。
- **目标**：建立可持续迭代的完成品表现基线：DOTween 驱动的界面/战斗反馈、完整胜负与奖励结算 UI、战斗信息显示、角色/背景/地块资源目录及符合规范的首批像素资源。
- **涉及文件/系统**：DOTween 包与初始化、Canvas 结算层、战斗命中/伤害/目标反馈、HUD 与地图推进信息、`64x64` 单位及 `32x32` 地块/图标资源、导入 QA、Play Mode 截图验证。
- **验收标准**：结算、奖励、地图推进和战斗 HUD 都具备清晰的信息层级与入场/退场/选择动画；攻击、命中、目标破坏和胜负提供可读反馈；角色、背景和地块使用可追踪的本地正式资源或合规占位资源，尺寸、透明边界、锚点、基线和 Point filter 通过 QA；1920×1080 与 960×540 无文字遮挡或元素溢出。
- **完成后解锁**：基于同一表现层持续扩展敌人、地图变体、事件和奖励内容。

### R1：肉鸽节点地图与战斗结算成长循环 - COMPLETE

- **归属**：肉鸽模式。
- **目标**：完成玩家推进单局流程的节点地图，以及战斗成功后的角色升级与“法术/武器混合池随机三选一”结算；奖励选择必须真实进入后续战斗构筑。
- **涉及文件/系统**：肉鸽地图节点状态/独立存档、单局种子随机序列、战斗结算、经验/等级、法术与武器奖励目录、构筑注入、地图与结算 UI、EditMode/Play Mode 验证。
- **验收标准**：地图有起点、两条可选推进路径、战斗节点和终点；仅已解锁节点可进入；战斗胜利回到结算并解锁后续节点；结算显示升级状态与 3 个随机候选，候选来自法术和武器池、三选一且可复现；选中后下一战实际装备对应法术/武器；失败、重开和剧情存档隔离正确；无时间压力规则。
- **完成后解锁**：扩展节点类型、事件池、奖励稀有度、地图变体与更多关卡。

### R0：最短完整肉鸽迭代基线 - COMPLETE

- **归属**：肉鸽模式。
- **目标**：以完成品标准建立可反复迭代的最短肉鸽闭环：第一关 → 单次事件 → 单次收获 → 角色升级 → 第二关 → 结算；每一阶段可读、可选、可恢复且真实影响后续战斗。
- **涉及文件/系统**：肉鸽运行状态/独立存档、两关任务目录、事件/收获/升级数据、战斗构筑注入、开发菜单与结算界面、EditMode/Play Mode 验证。
- **验收标准**：新开固定为两关；第一关胜利后依序进入事件、收获与升级，三项选择各有明确效果且升级影响第二关实际构筑；中途退出后可继续；第二关结算清楚汇总选择；失败、重开和剧情存档隔离保持正确；无任何时间压力规则。
- **完成后解锁**：围绕该闭环持续迭代数值、事件、奖励、地图变体与更多关卡，而不另起孤立原型。

### P3：肉鸽故事包与任务模板开发菜单入口 - COMPLETE

- **归属**：肉鸽模式。
- **目标**：交付可公开试玩的肉鸽前序版：并列剧情/肉鸽测试入口；肉鸽提供固定三任务故事链和歼灭/破坏模板沙盒，并注入现有简报、战斗、结算、重开和返回流程。
- **涉及文件/系统**：`RogueliteStoryPackage`、任务模板目录、独立肉鸽持久化、`CombatFlowController`、`CombatPrototypeBootstrap`、EditMode/Play Mode 验证。
- **验收标准**：肉鸽存档支持新开/继续/删除，且与剧情状态隔离；歼灭与破坏各有真实目标、胜负判定与结算；故事链胜利进入下一简报、失败可重开或返回菜单；无倒计时；1920×1080 验收无中文遮挡。
- **完成后解锁**：肉鸽完整流程与所需系统。

### P1-06：场景化战术 HUD 与固定像素图标 - COMPLETE

- **归属**：剧情模式与肉鸽模式共用。
- **目标**：将战术 HUD 的视觉结构、按钮和 `32x32` 像素图标从运行时绘制迁移为 `CombatPrototype` 场景中的固定 Canvas 层级与资产，脚本只进行状态绑定和交互转发。
- **涉及文件/系统**：`CombatPrototype.unity`、`TacticalHudSceneBinder`、`Icons32` 图标资产、Play Mode 验证。
- **验收标准**：
  - `场景UI/战术HUD` 保存完整的状态、指令、快捷栏、构筑/回合、行动条和记录层级；Canvas 上有固定 `TacticalHudSceneBinder`。
  - 6 枚指令图标作为 `Assets/Game/Art/UI/Icons32/` 的 `32x32`、Point filter 资产保存；不使用 AI 图图标或文字。
  - 保存的“攻击”场景按钮可在 Play Mode 切换实际战术选择；战斗开始时 HUD 同帧显示。
  - Funplay 编译错误为 0、Console 无项目错误；场景保存后通过 Git 记录。
- **完成后解锁**：P3 肉鸽故事包与任务模板开发菜单入口。

## 接下来

### P3：肉鸽故事包与任务模板开发菜单入口

- **归属**：肉鸽模式。
- **前置**：P0-05 完成。
- **目标**：让固定种子故事包及六类任务模板可以从开发菜单选择并进入共享战斗流程。
- **验收标准**：选择入口不污染剧情模式存档；任务目标和结算摘要来自数据定义；无时间压力字段。
- **完成后解锁**：开发垂直切片扩展测试。

### P0-03：工业城市区域循环原型

- **归属**：剧情模式。
- **前置**：P0-02 完成。
- **目标**：验证区域地图选点、关键地点自由探索、隐藏地点发现与已发现地点快速旅行。
- **验收标准**：据点服务入口、公开/隐藏地点、快捷路线和一条可回访支线均可验证；无时间消耗或敌情倒计时。

### P0-04：剧情模式存档、读档与任务准备流程

- **归属**：剧情模式。
- **前置**：P0-03 完成。
- **目标**：多槽存档、自动备份、读档、战术重开与战前配装/敌情/地图规则查看。
- **验收标准**：区域、战斗和装备状态独立保存恢复；失败后可重开或返回据点调整。

## 已完成 / 已知基线

- [x] 确定性单人战棋原型：行动点、行动条、朝向、掩体、伤害、状态、背包、快捷栏、构筑、敌人原型与 EditMode 测试。
- [x] 剧情模式方向：约 20 小时、三区域、自由探索/回访、无时间压力、完整存读档。
- [x] P0-01：通用可克隆任务目标，移除固定坐标胜利硬编码。
- [x] P0-02 至 P1-02：战役/地点/故事/版本状态、区域原型、存档备份、装备/服务与六类任务模板。
- [x] P1-03：固定种子、三任务链、结算摘要、独立存档的肉鸽故事包。
- [x] 运行时开发菜单、战前简报入口、正式战斗、战术重开、返回菜单及一轮 1920x1080 HUD 适配。

## 验证记录

| 2026-07-23 | V1-06 收口回归 | COMPLETE | 运行时断言通过：大地图节点选择、战前简报、正式战斗、强制胜利、肉鸽结算奖励领取、返回大地图；攻击/击破/可破坏物反馈对象成功生成。1920x1080 与 960x540 截图成功；场景 `isDirty=false`；Funplay 编译错误/警告 0。Console 仅已有 RenderTexture.active 警告。IMGUI 点击与独立 EditMode Runner 为工具限制，已有 EditMode 测试程序集保留作为覆盖证据。|

| 2026-07-23 | V1-06 最终回归 | PARTIAL PASS | Play Mode 运行时流程断言：战前简报、正式战斗、攻击反馈、战术重开、返回菜单、大地图入口均通过；节点完成与奖励领取通过；摄像机标签修复后 `Camera.main=present`；1920x1080 / 960x540 截图成功。Funplay 编译 0 错误/警告；Console 仅两条既有 RenderTexture.active 警告。IMGUI 控件不提供 uGUI 射线目标，`simulate_mouse_click` 无法证明鼠标命中；独立 EditMode Test Runner 不可用，保留现有 EditMode 测试程序集证据。|

| 2026-07-23 | V1-06 摄像机与流程补充 | PARTIAL PASS | 修复运行时未设置 MainCamera 标签的问题；Play Mode 断言 `Camera.main=present`、战前简报/正式战斗/战术重开/返回菜单/大地图入口均成功。点击工具在 IMGUI 画布采样点仍无 UI 命中，故保留为验证限制；编译错误/警告 0。|

| 2026-07-23 | V1-06 验收补充 | PARTIAL PASS | 场景信息确认 `Assets/Scenes/CombatPrototype.unity` loaded 且 `isDirty=false`；运行时断言节点完成后解锁并领取奖励成功；V1-04 反馈对象生成成功；Funplay 编译错误/警告 0。`simulate_mouse_click` 在退出 Play Mode 后按工具约束返回 PLAY_MODE_REQUIRED；聚焦 EditMode 无独立 MCP Test Runner，保留现有 EditMode 测试程序集作为覆盖证据。Console 有两条既有 RenderTexture.active 警告，无新增脚本错误。|

| 2026-07-23 | V1-04/V1-05/M1-01/M1-02/V1-06 统一验收 | PARTIAL PASS | V1-04 可破坏物与状态反馈、既有战斗 UI 基线、5 节点大地图数据与 relay_event 任务已完成；Play Mode 确认地图节点目录为 start/rail_patrol/relay_raid/relay_event/core_finale，反馈对象可运行时生成；1920x1080 与 960x540 截图均成功。Funplay 编译错误/警告 0，Console 仅既有弃用警告。完整鼠标逐项路径和 EditMode 聚焦执行仍待补充。|

| 日期 | 任务 | 结果 | 备注 |
| --- | --- | --- | --- |
| 2026-07-23 | V1-02b 结算奖励卡输入与悬停反馈 | COMPLETE | 将战术控制台的 Input System 兼容方案同步至 `RogueliteSettlementPresentation`：奖励卡由运行时鼠标命中兜底执行领取，不再依赖当前异常的 uGUI 射线命中；移除不兼容的 `StandaloneInputModule`，并为奖励卡增加 DOTween 悬停提亮与 1.025 缩放。Funplay 编译 0 错误/警告、Console 无错误；Play Mode 建立三选一结算后点击左侧奖励卡，`AwaitingReward` 由 `True` 变为 `False` 且领取 `aether_wand` 成功。未保存场景。下一步：V1-03 攻击源/目标/击破地图内视觉反馈与双分辨率验收。 |
| 2026-07-23 | V1-02a 战术控制台输入与悬停反馈 | COMPLETE | 定位到场景 HUD 缺少 `EventSystem`，导致 `GraphicRaycaster` 下的 uGUI `Button` 不能接收用户鼠标。`TacticalHudSceneBinder` 现于运行时补齐 `EventSystem`/`StandaloneInputModule`；为每个指令、快捷栏、构筑和回合按钮添加 DOTween 悬停亮色、1.035 缩放和按下缩放反馈，并提供 IMGUI 鼠标命中兜底以绕过当前 Canvas 射线命中异常。Funplay 编译 0 错误/警告、Console 无错误；Play Mode 点击“攻击”后状态切换为 `攻击`，再点“移动”切回 `移动`，按钮均实际命中；悬停触发器 4 项存在并可启动反馈 Tween。未保存场景。下一步：V1-03 攻击源/目标/击破的地图内视觉反馈与双分辨率视觉验收。 |
| 2026-07-23 | V1-02 肉鸽结算层与可重复胜负反馈 | COMPLETE | 新增运行时 `RogueliteSettlementPresentation`：战斗胜利后以 DOTween 进入的全屏结算层展示等级、经验、三张武器/法术奖励卡、伤害/射程/穿甲或耗能数值；卡片点击后真实领取、保存并关闭结算层。`CombatVisualFeedback` 在开始/战术重开时清空结局去重与生命缓存，允许同一会话连续多场胜利/失败都有反馈。Funplay 编译 0 错误/警告、Console 为空；Play Mode 中确认结算 Canvas 创建，并用真实 UI 点击领取 `aether_wand` 后 `AwaitingReward=False`、已领取列表正确；新增 EditMode 奖励数值可展示测试并通过聚焦检查。未保存场景。下一步：V1-03 接入攻击源/目标/击破的地图内表现，并完成 1920x1080 与 960x540 可视化验收。 |
| 2026-07-22 | V1-01 DOTween 与表现基础 | COMPLETE | 已从本机现有 Unity 项目引入完整 DOTween 插件目录并完成 Funplay 域重载；编译 0 错误/警告。新增运行时战斗结果与伤害浮字反馈组件，以及地图面板可见性保护。首份本地生成角色原料已去除绿幕，但 QA 为 1024×1024、约 4 万色，不符合 64×64/受控调色板正式资源标准，已隔离至 `Art/Raw/Units`，未导入 Unity 正式资源目录。下一步：制作并 QA 首批 64×64 单位与 32×32 地块/背景资源，再接入正式视觉层。 |
| 2026-07-22 | R1 | COMPLETE | 肉鸽节点地图包含起点双路径（歼灭/破坏）和终点；战斗胜利回到地图结算、获得经验并升级，展示由法术/武器混合池确定性抽取的 3 个候选且可三选一；选择注入下一战构筑。EditMode 9 项通过；Funplay 编译 0 错误/警告、Console 无错误；Play Mode 验证胜利后等级 2、3 个奖励候选、选择法术后注入终点战斗。未保存场景。 |
| 2026-07-22 | R0 | COMPLETE | 最短肉鸽已收敛为两关完整循环：第一关歼灭 → 现场修复事件(+1 护甲) → 护盾电池收获(额外快捷栏) → 校准步枪升级(伤害 5) → 第二关破坏 → 结算。阶段状态独立序列化并可继续；三项选择均实际注入第二关。EditMode 7 项通过；Funplay 编译 0 错误/警告、Console 无错误；Play Mode 验证第二关构筑为护甲 2、`calibrated_rifle` 伤害 5、额外 `shield_cell`，并完成第二关结算。未保存场景。 |
| 2026-07-22 | P3 | COMPLETE | 决策答卷已固化：并列肉鸽测试入口、随机种子三任务链、歼灭/破坏沙盒、独立肉鸽存档、新开/继续/删除、故事链胜利推进、失败返回配置、战术重开与一键胜负测试入口。Funplay 编译 0 错误/警告、Console 无错误；Play Mode 验证故事链首战胜利进入工厂突破简报、两个沙盒目标与失败返回。未保存场景。 |
| 2026-07-22 | P0-01 | COMPLETE | 通用可克隆目标；Funplay 编译无错误，EditMode 通过。 |
| 2026-07-22 | P0-02 至 P1-02 | COMPLETE | 状态、区域、存档、服务、任务模板实现并通过 EditMode/Funplay 编译。 |
| 2026-07-22 | P1-03 | COMPLETE | 肉鸽故事包独立存档；EditMode 通过，Funplay 编译无错误。 |
| 2026-07-22 | 开发菜单与正式战斗流程 | COMPLETE | Play Mode 菜单与 start/back 流程验证；未保存场景。 |
| 2026-07-22 | UI 美化与屏幕适配 | COMPLETE | 75% 战场 + 25% HUD；CellSize 调整为 78 以避免 960x540 越界。 |
| 2026-07-22 | 本地像素素材生成与审查 | BLOCKED | Relay Canvas 有本地 API Key，但上游 gpt-image-2 负载饱和，未导入任何 AI 图或正式资产。 |
| 2026-07-22 | P0-05 | COMPLETE | `CombatFlowController` 新增菜单/简报/结算重开/返回菜单状态；运行时 Bootstrap 改由单一流程驱动。Funplay 编译错误 0，聚焦 EditMode 通过；Play Mode 验证菜单->简报->战斗、胜利/失败结算、战术重开和返回菜单，主角重开恢复至 (1,4)。1920x1080 与 960x540 菜单截图无越界；未保存场景。下一步：P3 开发菜单故事包/任务模板入口。 |
| 2026-07-22 | P1-04 | COMPLETE | 控制台改为 500px 宽的分区侧栏：资源条摘要、双列战术指令、快捷栏、构筑/回合、精简行动条与战斗记录。Funplay 编译无错误、Console 无项目错误；Game View 实测无文字重叠，未保存场景。 |
| 2026-07-22 | 本地 UI 方向图 | PASS | 已启动 `E:\数据库\图片生成` Relay Canvas 并通过本机接口生成 UI 概念参考图：`E:\数据库\图片生成\outputs\gpt-image-2-2026-07-22T12-24-18-570Z-4dcd4cf3.png`。图仅用于布局/材质/色彩审查，未导入 Unity。 |
| 2026-07-22 | P1-05 | COMPLETE | 实装黑白极简工业战术 HUD：细线框架、灰阶面板、限量冷青/锈红/安全黄语义色，以及运行时生成的 6 枚独立 32x32 点过滤像素指令图标。Funplay 编译无错误、Console 为空；1920x1080 Game View 与 960x540 截图确认 HUD 可见且无重叠/越界；未保存场景、未导入 AI 图。下一步：P3 肉鸽故事包与任务模板开发菜单入口。 |
| 2026-07-22 | P1-06 | COMPLETE | HUD 已场景化：`CombatPrototype` 的 `场景UI/战术HUD` 保存了全部细线分区、`Button`、`RawImage` 图标槽、资源条和行动条，Canvas 保存 `TacticalHudSceneBinder`。6 枚固定 32x32 Point-filter 图标资产保存到 `Assets/Game/Art/UI/Icons32/`；Play Mode 验证 HUD 同帧激活及“攻击”按钮实际切换选择。编译无错误、Console 无错误。 |
