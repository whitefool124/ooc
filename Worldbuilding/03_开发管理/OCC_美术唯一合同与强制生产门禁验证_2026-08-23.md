# OCC 美术唯一合同与强制生产门禁验证（2026-08-23）

## 目标与边界

- 将 `Worldbuilding/05_美术与音频/OCC_美术规范_v0.1.md` 确立为 OCC 唯一美术规范源。
- 把尺寸路由、来源、应用占格、关键材质色、QA证据、人工审美和状态晋级变为可执行合同，避免每批依赖临时解释。
- 本轮只修改 `AGENTS.md`、Worldbuilding规范／记录和 `Tools/OCCArt` 离线工具；未修改、打开或导入 `UnityProject/Assets`，未保存场景。

## 已清除的冲突

1. 旧“全部图标／地块32×32”改为16px语义、24px材质源无缩放进入32px槽、32px装备／单格、`(W×32)×(H×32)`多格、64px单位的唯一角色路由。
2. 旧“所有对象恰占一格”失效；多格地图物件必须登记逻辑占格并按完整画布检查中线与碰撞诚实性。
3. 单位标准人形统一为44–48px、目标46px，精英／首领48–54px，四足宽52–62px／高26–36px；清除单位规范中的32–38px旧值。
4. 当前Unity交付PPU统一为语义16px使用PPU16，其余32px格体系（含64px单位、多格物件、24→32交付）使用PPU32；24px原生材质稿不直接进入当前正式槽位。
5. AI规则统一为“批准的直接生成只可产生可追溯独立原料”；机器QA、应用接触和人工审美通过后仅为 `FORMAL_CANDIDATE`，完成稳定GUID／Importer／运行时复核后才是 `FORMAL`。
6. 历史 `ARTIFACT-PACK-20/artifact_imagegen_manifest_v1.json` 已标记 `HISTORICAL_PROVENANCE_ONLY / executable=false / normative=false`，保留历史事实但不能作为现行入口。

## 强制生产入口

- 机器镜像：`Tools/OCCArt/occ_art_contract_v1.json`
- 新资产模板：`Tools/OCCArt/occ_art_manifest_template_v1.json`
- 单项验证：`Tools/OCCArt/validate_occ_art_asset.py`
- 全库审计：`Tools/OCCArt/validate_all_occ_art.py`
- 回归测试：`Tools/OCCArt/test_occ_art_contract.py`

验证器检查：规范一致性、批准来源、禁用路径、源／成品哈希、角色尺寸、逻辑占格、硬Alpha、调色板、安全边、24→32无缩放嵌入、关键材质最小像素、1×／4×／灰阶／棋盘格／应用接触证据、人工五维审美和Unity正式晋级字段。

## 正向回归

| 资产 | 角色 | 结果 | 关键指标 |
|---|---|---|---|
| `academy_communal_wood_table` | `single_cell_prop_32` | PASS | 32×32、8色、bounds `[3,13,29,28]` |
| `seasoning_bottle` | `material_pickup_24_to_ui32` | PASS | 24→32严格嵌入、7色、ochre `207px` |
| `wooden_sapphire_staff` | `equipment_icon_32` | PASS | 32×32、8色、blue `9px`（最低4px） |
| `academy_dormitory_bed` | `multi_cell_prop_32` | PASS | 64×32、逻辑 `[2,1]`、10色、blue `361px` |

全库审计：`manifest_count=4 / pass=4 / fail=0`。

## 负向回归

`Tools/OCCArt/test_occ_art_contract.py` 共6项全部通过，其中稳定验证：

- 法杖蓝色最低像素阈值无法满足时失败，防止关键宝石被限色吞掉。
- 64×32床伪报为32×32单格物件时失败，防止多格资产被强塞单格。
- `local_workbench`／`localhost`来源同时触发未批准渠道和禁用路径失败。
- `FORMAL_CANDIDATE`缺少人工总评或应用维度PASS时失败。
- 四项真实manifest及总规范一致性审计全部PASS。

## 能保证与不能保证

- 不能保证生成模型每次第一稿都具有相同审美质量。
- 可以保证：只要执行仓库规定入口，任何尺寸／占格／来源／关键材质／证据或人工审美不合格的资产都无法通过机器合同，也不能合法晋级为正式Unity资源。
- 风格判断仍必须有人审；机器门禁负责把可客观化的失败前移，并阻止“机械PASS代替美术放行”。

## 最终状态

- 合同审计：PASS。
- 四项正向资产：4/4 PASS。
- 工具回归：6/6 PASS。
- `git diff --check`：PASS；无新增空白错误。
- Unity／Funplay：不适用；本轮未修改Unity脚本、数据、资产或场景。
