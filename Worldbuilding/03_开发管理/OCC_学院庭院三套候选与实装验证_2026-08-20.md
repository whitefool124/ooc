# OCC 学院庭院三套候选与实装验证（2026-08-20）

## 结论

本轮不是对旧稿换色，而是独立生产三套地板家族、每套 A–D 共 12 张：`large_flags`（大块学院石板）、`cloister_courses`（回廊条石）、`old_slate`（旧馆磨石）。最终实装二次结构性重绘后的 `large_flags`；另两套保留为可复跑的正式备用，不进入当前运行时稳定 ID。

## 实装方案

- 四份源稿均为独立手绘 `16×16` 文本像素网格，最近邻 2× 输出为 `32×32` PNG。
- 初版 `large_flags` 的圆角与四边起伏在混铺时产生按钮阵列感，因此没有直接实装。二次重绘取消圆角、上／左包框和闭合倒角，只在每块下沿／右沿绘制浅灰石缝、局部缺口与旧填缝；边界完全来自贴图自身接触边。
- A–D 可见色数为 `5 / 6 / 6 / 5`，Alpha 仅 `255`，严格 `16×16→32×32` 最近邻 2×，四张哈希互异；平均明度为 `134.37 / 134.12 / 134.17 / 134.25`，家族最大差 `0.25`。
- 正式路径保持 `UnityProject/Assets/Game/Resources/Art/FormalAcademyCombat32/academy_courtyard_[a-d].png`，原 `.meta`／GUID 与运行时映射不变。

## 备用分流

- `cloister_courses`：`FORMAL_BACKUP`。暖色回廊条石成立，但内部横向层缝在当前方格战场中容易误读成墙砖和第二套网格。PNG 位于 `alternates/terrain_candidates_v05/cloister_courses/`。
- `old_slate`：`FORMAL_BACKUP`。旧馆深灰磨石成立，但当前庭院中偏暗且磨痕较抢，适合旧馆／地下层／阴凉室内。PNG 位于 `alternates/terrain_candidates_v05/old_slate/`。
- 三套独立源稿均保存在 `source/terrain_candidates_v05/`，比较图与混铺图保存在 `QA/terrain_candidates_v05/`；被替换的 v04 正式稿存档于 `rejected/terrain_runtime_clean_heritage_before_v05/`。

## Unity 门禁

- `Application.dataPath = E:/数据库/OCC_Codex/UnityProject/Assets`；Editor 非 Play Mode，替换前后均为 `0` 个 dirty scene。
- 四张 Importer 读回一致：`TextureType=Sprite`、`FilterMode=Point`、`WrapMode=Clamp`、`TextureCompression=Uncompressed`、`mipmapEnabled=false`。
- 编译：`0 error / 0 warning`；Console：无 error。
- `OCC.Combat.EditModeTests`：`580 passed / 0 failed / 0 skipped`。
- 本轮未保存场景、未进入 Play Mode、未修改运行时代码、地图、PPU、玩法或数值。
