# OCC 学院庭院地块资产返工验证（2026-08-20）

## 结论

`academy_courtyard_a/b/c/d` 已完成正式返工，并在后续玩家审阅后进一步降为 `16×16` 逻辑像素、最近邻 `2×` 的 `32×32` 交付稿。普通格位边界由地块自身的石质灰缝、阶梯倒角和边缘明暗表达；未恢复 Unity `Outline`、矢量描边或独立常驻网格线。最终数据见 `OCC_学院庭院16逻辑像素验证_2026-08-20.md`。

## 资产结果

- 稳定路径：`UnityProject/Assets/Game/Resources/Art/FormalAcademyCombat32/academy_courtyard_a-d.png`。
- 原生规格：四张均为 `32×32` RGBA、全不透明、6 色、独立哈希。
- 色板角色：深灰缝、石材暗面、暖灰基底、浅石面、磨亮面、棕灰修补。
- 家族平均明度：A `136.19`、B `135.16`、C `136.11`、D `136.17`；最大差 `1.03`。
- QA：`Worldbuilding/05_美术与音频/正式美术生产/M-A18/QA/terrain_rework_v02_hand_authored_family/academy_courtyard_abcd_contact_4x.png` 与 `academy_courtyard_mixed_12x9_2x.png`。
- 可复跑源：四份 `.pxart` 位于 `source/terrain_rework_v02_hand_authored/`；`tools/export_pxart32.py` 只导出显式像素，不生成边缘或材质结构。

## 失败稿与回退

- 生成探索的格内裂缝／围框／近似两色／噪点版本保留为 `rejected/terrain_rework_pass01/` 和 `rejected/terrain_rework_pass02/`，未作为正式资产。
- 覆盖前的旧运行时 PNG 保存在 `rejected/terrain_runtime_before_rework_20260820/`，可按文件逐张人工回退；正式 `.meta` 未替换，GUID 保持不变。

## Unity 验证

- 身份门禁：`Application.dataPath = E:/数据库/OCC_Codex/UnityProject/Assets`；活动场景 `Assets/Scenes/CombatPrototype.unity`。
- 四张 Importer 读回：Texture Type `Sprite`、Filter `Point`、Wrap `Clamp`、Compression `Uncompressed`、mipmap disabled。
- 编译：`0 error / 0 warning`。
- EditMode：`580/580 passed`。
- Console：无 error，当前为空。
- 场景：`0 dirty scene`；未保存场景、未进入 Play Mode。

## 未做事项

本轮没有修改地图拓扑、地块 ID、碰撞、选格、范围覆盖、输入、数值或存档，也没有自动返工其他地块族。下一步需先在真实战斗画面检查单位／范围覆盖层与新石地的主次关系。
